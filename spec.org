#+TITLE: standard-change — governing spec
#+SUBTITLE: Label-driven, gate-checked deployment pipeline with a mock storefront monorepo
#+AUTHOR: aygp-dr
#+DATE: 2026-09-12
#+PROPERTY: header-args :mkdirp t
#+OPTIONS: toc:2 num:t

* Status

- Version: 0.5 (establishing; deviations recorded in §Version. NOTE: issue #1
  was the review ledger and is now the ESTATE STATE holder -- freeze and
  emergency live there. Deviations go in the commit and in §Version)
- Working repo name: =standard-change= (ITIL 4: a pre-authorized, low-risk change verified by fixed gates). Alternatives considered: =change-schedule=, =change-window=, =slipway=, =gatehouse=.
- This file is the single canonical document. =CLAUDE.md= and =README.org= are derived and restate only what an agent or reader needs to act.

** Epistemic labels

Claims that are not self-evident carry a label. A claim with no label is
structural (a definition or a decision), not an observation.

| label | meaning                                                       |
|-------+---------------------------------------------------------------|
| [E]   | established — observed on a named cell, with the run recorded  |
| [P]   | provisional — working assumption, expected to be revisited     |
| [H]   | hypothesis — believed, not yet observed; never a bare claim    |
| [I]   | inferred — derived from other claims, not directly observed    |

** Changelog

*Declared deviation, recorded rather than tidied.* The version sat at 0.3 from
the spec's first commit through fifteen commits of material change, including
five changes to guard behaviour and three corrections to claims the spec had
already published. It should have been bumped at each. 0.4 therefore
accumulates what ought to have been several revisions, and the discipline in
§Status was not followed until now.

*** 0.4 — 2026-09-13

Structural:
- §Domain model — the entities and all six guards restated as platform-neutral
  predicates, with GitHub label names demoted to an /encoding/ column, plus
  capabilities C1–C6. This is the change that makes the rest portable.
- §The balance — production is the objective; the queue and calendar are
  instruments. Adds refutation condition 8.
- §At scale, §Controls audit, §What completes a deployment.

Guard behaviour (each driven by an observation, not an argument):
- Guard 4b classifies divergence (=inert= / =pipeline= / =artifact= / =hotfix=)
  instead of treating any trunk movement alike, and *never takes the berth for
  someone else's merge*.
- Guard 4b is evaluated at three points: trunk movement, deploy, and *merge*.
  The merge check came from =sim/= at =berths=2=.
- Guard 2 is evaluated on the emergency path too. It had lived only in
  =promote.yml=, which the emergency path never reaches — the spec claimed a
  property the workflows did not implement.
- Guard 4 is re-checked at merge. Found by TLC once deploy and merge were
  separate steps.
- Guard 5 asserts *convergence*, not liveness: N samples, one request each,
  cache-busted, with an explicit =UNCONVERGED= verdict.

Corrections to published claims:
- The Deployments API is *not* platform-attested; C6 is unmet on GitHub.
- GitHub Pages *cannot* set custom response headers, so guard 5 needed a
  =manifest= mode; the spec had required =x-build-sha= on a target that cannot
  provide it.
- =ports.tsv= is mutable state and is no longer a =:tangle= target.

Not carried into this version, and still open: *A5, "deployment is undoable"* —
an axiom none of the six guards encodes. Nothing in the model has a state
dimension, so an expand/contract migration or a published library version
falsifies the rollback assumption that guard 5's failure path relies on.

*** 0.3 and earlier

0.1 the project statement; 0.2 the promotion pipeline, owned labels and guards
0–5; 0.3 guard 4b, after scenario D4. Recorded in git history and
=experiments/001=; not reconstructed here.

* Purpose

The repo demonstrates change enablement and deployment management on a mock ecommerce monorepo. The apps are fixtures. The product is the gate sequence, the labels that drive it, the change-schedule integration, and the checklists.

Two pre-conditions gate every production deployment:
1. No other deployment is in progress (deployment lock).
2. No emergency change or change freeze is active on the change schedule.

* What this model gets wrong, on purpose

*The architecture being simulated here is a bad one, and the pipeline is the
cost of it.* Stating that plainly is load-bearing: everything downstream —
the singleton berth, the queue, guards 0, 1 and 4b, the rebase loop, the
preemption rules — is machinery for surviving a missing contract, not a
pattern to copy.

The defect: four apps share one reverse proxy and one mock backend with *no
enforced interface between them*, and there is a single path to production. So
any change to any app might break any other, and the only available remedy is
to serialize: one change at a time, tested against current production, promoted
whole. The queue is not a design, it is what you are left with when nothing
else can tell you whether two changes compose.

What the right shape would be [I]:

- *Hard contracts at the router.* Route ownership declared and enforced, with
  versioned, consumer-driven contracts between apps rather than an implicit
  agreement that the storefront hangs together. Contract tests then answer
  "do these compose?" statically, per app, in seconds.
- *A path to production per team.* Each team owns its own production
  deployment for its own app, gated by its own contract tests. No shared berth,
  so no queue, so no guard 0, no guard 1, no guard 4b, no preemption rules, no
  rebase loop. Scenario D4 becomes unstateable rather than defended against.
- *Staging stops being scarce*, because it stops being the only place where
  cross-app truth is discovered.

So read the guard sequence as a /faithful model of a common failure/, not as a
recommendation. Organizations really do run this way, the coordination costs
really do land where this spec puts them, and the reason to build it carefully
is that the machinery is load-bearing exactly as long as the contracts are
missing. A reader who takes the pipeline home without noticing the note has
taken home the wrong artifact.

*Non-goal:* fixing it. This repo demonstrates change enablement under a shared
path to production. Replacing that with per-team contracts would be a different
repo, and a shorter one.

* Domain model

*The workflow is generic; the semantics are the product.* This section states
the model without reference to GitHub, because a guard that can only be
expressed in one vendor's vocabulary is a guard we have not understood. The
label names elsewhere in this document are one /encoding/ of what follows, not
the thing itself.

** Entities

| Entity                | Definition                                                                 | GitHub encoding                    |
|-----------------------+----------------------------------------------------------------------------+------------------------------------|
| *change*              | a unit of intent, with an author, a base, and a head                        | pull request                       |
| *manifest*            | the set of deployable components a change touches; /derived/ from the diff  | =app:*= labels, labeller-owned     |
| *gate*                | a function from a content hash to a verdict in {pass, fail}                 | a check run                        |
| *suite*               | all gates; green iff every member is green /and/ the self-test is green     | the required-checks set            |
| *berth*               | an exclusive, deadline-bearing lease on the path to production              | the =deploy:staging= label + lock  |
| *path to production*  | the ordered environments a change traverses: staging → production           | environments                       |
| *divergence*          | the classified relation between a change's base and the trunk tip           | computed by =divergence.sh=        |
| *authorization*       | normal (earned by gates) or emergency (granted by a person)                 | =change:emergency=                 |
| *deployment record*   | platform-attested statement that artifact X reached environment Y           | the Deployments API                |

Two of these are load-bearing and routinely got wrong:

- *manifest is derived, never asserted.* It is a function of the diff, owned by
  automation, and a human edit to it must not survive. If it can be asserted,
  it is not a manifest — it is a wish, and the build ships whatever someone
  typed. (Scenario L10.)
- *deployment record is attested, not self-reported.* The difference is who
  says it happened: the platform, or the job that wanted it to. A job claiming
  its own success is not evidence.

  *And GitHub's Deployments API does not meet this bar* [E, 2026-09-13]. A
  deployment is created by a caller (=creator= is a user or app) and its status
  is POSTed to =statuses_url= by that same caller; with Actions environments
  the state mirrors *the job's* status. GitHub never contacts the target. So
  the row above records intent, exactly like a Release does — it is a
  self-report with good provenance about *who claimed it*, not evidence about
  *what is running*. Capability C6 is therefore *unmet on GitHub*, not merely
  awkward, and guard 4 rests on guard 5's sampling instead.

** The guards as predicates

Let =H(c)= be a change's head hash, =B(c)= its base, =T= the trunk tip,
=merges(x,y)= the merges on trunk in =(x, y]=, and =class(m)= the divergence
class of a merge.

| Guard | Predicate                                                              | Evaluated                 |
|-------+------------------------------------------------------------------------+---------------------------|
| *0*   | =B(c) = T=                                                             | at berth claim            |
| *1*   | =|{x : holds_berth(x)}| ≤ 1=                                           | continuously              |
| *2*   | =∀g ∈ suite : g(H(c)) = pass=, and =selftest(H(c)) = pass=             | at promote *and* at deploy |
| *4*   | =staged(H(c)) ∨ (emergency(c) ∧ approved(c))=                          | at deploy                 |
| *4b*  | =∀m ∈ merges(B(c), T) : class(m) ∈ {inert, pipeline}=                  | *at reliance*, not request |
| *5*   | =served_build(production) = H(c)=                                      | after deploy              |
| *6*   | =deploys(c) = ∅ ∨ served_build(production) = T=                         | at merge                  |

Guard 2 appears twice on purpose (§Emergency — the only bypass). Guard 4b's
"at reliance" is the whole content of the rule: *a guard whose subject can
change after it is checked must be re-checked at the moment it is relied on.*

*** Guard 6 (production-first), and why it is stated here rather than assumed

Added 2026-09-14 after it was found missing. Guard 6 — "do not merge something
new onto a trunk that production is not yet serving" — was implemented in this
repo and independently in the =000= rebuild, and *is wrong in both*, by the same
mechanism. It appeared in no predicate here, so both implementations invented it
and both invented it the same broken way.

The failure is in =deploys(c)=, not in the comparison. Both derived the deploy
set and then treated an *empty* set as *exempt*:

- here: =groups.sh "$pr" 2>/dev/null || true= — a forge that could not answer
  produced the empty set, and the guard exempted the change. 484 runs, never
  once triggered by a label.
- in =000=: =prodfirst: exempt -- this change deploys nothing (derived from the
  diff, not from a label)= returned exit 0 for both of the cases the guard exists
  for, including the one where production could not be reached at all.

*The rule, and it is the whole point of stating the guard:* =deploys(c)= is
three-valued, never a set that may be empty.

| =deploys(c)= | meaning | guard 6 |
|--------------+---------+---------|
| a non-empty set | this change deploys these groups | compare; refuse unless production serves =T= |
| *provably* empty | the diff was read and touches nothing deployable | exempt, and say which oracle proved it |
| indeterminate | the diff or the labels could not be read | *abstain* — exit 4, blocks |

/Exempt/ requires positive evidence that the change deploys nothing. The absence
of evidence that it deploys something is *indeterminate*, and indeterminate is
not exempt. This is defect class 1 (unreachable is not falsified) and class 8
(cannot say is not no) applied to the one guard that had no predicate to defend.

A conforming implementation must have a test that fails when the guard is
removed, for all three rows — =000='s selftest had exactly the right two cases
and the guard exempted its way past both, which is why the case list matters as
much as the predicate.

**** What =000= could and could not tell us, stated separately

=000= has no port sweep, no router and no running apps — the 9000-series block
model is not in it. So only one of the two failures is evidence about the guard:

- *Evidence.* =prodfirst ABSTAINS when production cannot be reached= returned
  /exempt/ (0) instead of /abstain/ (4). The exempt short-circuit is evaluated
  BEFORE reachability, so the guard answers "nothing to check" without ever
  having tried to look. That is an ordering defect in the code path and it does
  not need an estate to be true. Our =groups.sh ... || true= is the same
  ordering, reached a different way.
- *Not evidence.* =prodfirst refuses: production is running another build=
  cannot be exercised without a production to observe. =000= faked it in
  process, so what failed there is a fixture's guess, not a measurement. It is
  recorded as UNDERIVABLE rather than counted against the rebuild.

This is itself a conformance requirement, and it belongs with the guard: *guard
6 cannot be tested without a real estate.* A pipeline that can only test it
against an in-process double has not tested it — the same distinction §Targets
draws between the deployed thing and the tested thing. An implementation without
the port-block model can state guard 6 and cannot verify it, and must say so.

** Conformance — what a platform must provide

The guards reduce to six capabilities. This is the checklist for any platform,
and the thing to confirm early in a rollout:

| # | Capability                                                        | Needed by     | Rarity                      |
|---+-------------------------------------------------------------------+---------------+-----------------------------|
| C1 | mutable, queryable state on a change, *owned by automation*      | manifest, all | common                      |
| C2 | verdicts bound to a *content hash*, not a branch or a change id  | guard 2       | common, often misconfigured |
| C3 | an *exclusive lease* with owner and deadline, readable by others  | guard 1       | uncommon                    |
| C4 | an *event when the trunk moves*                                   | guard 4b      | *rare*                      |
| C5 | verdicts that are *revocable after issue*                         | guard 4b      | *rare*                      |
| C6 | a *platform-attested* deployment record                           | guard 4       | uncommon                    |

C4 and C5 are the expensive ones, and they are both guard 4b's — which is
consistent with §strict does not protect production, where no ruleset rule
could express it either. *If a platform gives you only one thing, take C2;
if it gives you two, take C2 and C5.* A verdict you cannot revoke is a
verdict that outlives its own truth.

C2's failure mode deserves naming because it is silent: bind a verdict to a
branch or a change id instead of a hash and everything still appears to work,
right up until the head moves and a stale pass authorizes a deploy. That is
scenario D4's mechanism with a different cause.

** What this buys

Stating the model this way is what makes the rest portable. =spec.org='s
labels, =scenarios.org='s cases, =tla/='s invariants and
=gates/pbt-pipeline.py='s model all reduce to the predicates above; the
GitHub-specific parts are the /encoding/ column of the first table and nothing
else. A port is then a conformance exercise — check C1–C6, encode the six
predicates — rather than a rewrite.

* Vocabulary (ITIL 4)

| Term                          | Meaning in this repo                                                                  |
|-------------------------------+---------------------------------------------------------------------------------------|
| Change                        | One pull request with at least one =app:*= label                                       |
| Standard change               | A PR whose diff touches only pre-authorized paths and passes all gates; no CAB review  |
| Normal change                 | A PR touching =router/=, =gates/=, =change/=, or =.github/=; requires one human approval |
| Emergency change              | A calendar event titled =EMERGENCY: ...= or repo variable =EMERGENCY_CHANGE=1=          |
| Change freeze                 | A calendar event titled =FREEZE: ...= covering the requested window                   |
| Change schedule               | The Google Calendar used for change windows, freezes, and emergencies                 |
| Change window                 | A calendar event =CHANGE: <repo>#<pr> <group>= created before staging deployment       |
| Change authority              | The gate workflow for standard changes; a named reviewer for normal changes           |
| Deployment                    | One run of =deploy-staging.yml= or =deploy-production.yml=                              |
| Release                       | A production deployment whose PIR comment is posted                                   |
| Service validation and testing| The three gates: lint, unit test, Playwright                                          |
| Post-implementation review    | Auto-generated PR comment after production deployment with gate results and timings   |
| CMDB                          | =ports.tsv= (worktree ports) and =environments.tsv= (staging slots)                    |

** This spec is intent, not source

*Nothing here tangles.* Every code block is a *reference implementation* — it
shows one way to satisfy the requirement around it, and the file on disk is the
source of that file.

The reason is the one §Domain model already argues for the guards: the spec
states /what must hold/, and an encoding is free. An agent handed this document
should be able to implement =gates/health.sh= in Python, or the labeller as a
different action entirely, and be *right* so long as the predicate holds. A
tangling spec forecloses that — it hands over bytes and calls them a
requirement.

It also removes a defect this repo hit twice: a change edited directly into
=Makefile=, committed, and then silently reverted by the next tangle. When the
spec owns the bytes, every derived file is a place for the document and the
repository to disagree without anyone noticing.

*So: read the prose for the contract, read the block for an example, and read
the file for what actually runs.*

* Repository layout

#+begin_example
standard-change/
├── spec.org                     this file
├── CLAUDE.md                    derived
├── Makefile                     dev, gate, simulate, port targets
├── ports.tsv                    worktree → port block registry
├── environments.tsv             staging slot registry
├── apps/
│   ├── core/                    /, /login, /account, /cart
│   ├── plp/                     /search, /c/:category
│   ├── pdp/                     /p/:sku
│   └── checkout/                /checkout, /checkout/payment, /checkout/confirm
├── router/
│   ├── routes.json              generated: union of apps/*/routes.json
│   ├── nginx.conf.tmpl          worktree and staging router
│   └── worker.js                Cloudflare router Worker (same routes.json)
├── tests/
│   └── e2e/                     cross-app journeys (add-to-cart → cart → checkout)
├── gates/
│   ├── lint.sh
│   ├── test.sh
│   ├── e2e.sh
│   └── preflight.sh             lock + freeze + emergency checks
├── change/
│   ├── schedule.sh              calendar block / query
│   ├── lock.sh                  deployment lock acquire / release
│   └── pir.sh                   post-implementation review comment
├── targets/
│   ├── github/                  Environments + Deployments API + Pages
│   └── cloudflare/              Workers per app + router Worker
└── .github/workflows/
    ├── labeller.yml
    ├── gate.yml
    ├── deploy-staging.yml
    └── deploy-production.yml
#+end_example

* Apps

** Deploy groups

A deploy group is one app directory. =core= is the default group; aliases =main= and =default= resolve to =core=.

| Group    | Directory       | Routes                                        | Playwright journeys              |
|----------+-----------------+-----------------------------------------------+----------------------------------|
| core     | =apps/core=     | =/=, =/login=, =/account=, =/cart=            | home, login, account, cart       |
| plp      | =apps/plp=      | =/search=, =/c/:category=                     | search                           |
| pdp      | =apps/pdp=      | =/p/:sku=                                     | pdp, add-to-cart                 |
| checkout | =apps/checkout= | =/checkout=, =/checkout/payment=, =/checkout/confirm= | checkout                 |

=add-to-cart= starts on a pdp page and asserts on =/cart=, so it is owned by =pdp= and also runs in =tests/e2e/= as a cross-app journey.

** Per-app structure

Every app has the same shape. The gate scripts and the labeller depend on this shape.

#+begin_example
apps/<app>/
├── routes.json            route ownership; input to router/ and to labeller
├── package.json           scripts: dev, build, lint, test, e2e
├── src/
│   ├── server.js          node http server; reads PORT
│   └── pages/             one file per route
├── tests/
│   ├── unit/              node:test
│   └── e2e/               Playwright specs for this app's journeys
├── fixtures/              catalog.json, cart.json, users.json
└── playwright.config.ts   baseURL from ROUTER_URL; projects = this app's journeys
#+end_example

** routes.json contract

#+begin_src json
{
  "app": "plp",
  "port_offset": 2,
  "routes": ["/search", "/c/:category"],
  "health": "/search?q=ping",
  "journeys": ["search"]
}
#+end_src

=port_offset= is fixed per app: core 1, plp 2, pdp 3, checkout 4. Offset 5 is reserved for a mock backend (catalog/cart API) shared by all apps.

** Simulated changes

=make simulate app=<group>= appends a line to =apps/<group>/src/CHANGELOG.txt= and bumps =version= in =package.json=. The diff is enough for the labeller to attach =app:<group>=. No behavior changes; the point is exercising the pipeline.

* Router and ports

** Port block model

Each git worktree owns a contiguous block of ten ports. The router (nginx) listens on the block base; apps listen on base + offset.

| Worktree              | Block | router | core | plp  | pdp  | checkout | mock |
|-----------------------+-------+--------+------+------+------+----------+------|
| main (stable)         |     0 |   9000 | 9001 | 9002 | 9003 |     9004 | 9005 |
| first branch worktree |     1 |   9010 | 9011 | 9012 | 9013 |     9014 | 9015 |
| second                |     2 |   9020 | 9021 | 9022 | 9023 |     9024 | 9025 |
| n-th (n ≤ 9)          |     n | 9000+10n | +1 | +2   | +3   | +4       | +5   |

Offsets 6–9 in each block are unallocated. *Block 9 is the last*: block 10
would start at 9100, which is the team tier (§The port map).

** The port map

One picture, because three schemes coexist and only one of them can collide.

#+begin_src mermaid :eval never-export
flowchart TB
    subgraph lan["reachable from 192.168.86.0/24 — pf passes 7000-7699, 8000-9999"]
        subgraph ours["9000-9299 — this project"]
            W0["9000-9009  worktree block 0"]
            W1["9010-9019  block 1"]
            WN["9090-9099  block 9  (the ceiling)"]
            TE["9100-9199  team envs — cannot promote"]
            FP["9230  production front -> live colour"]
            FS["9200  staging"]
        end
        OTHER["8080 dump1090 · 8081 · 8083 · 7500 · 7510 · 7680 · 7681 · 7747 · 9630<br/>OTHER SERVICES — not ours"]
    end
    subgraph jails["10.0.0.0/24 — jails, not LAN reachable"]
        J1["sc-staging  10.0.0.60:80"]
        J2["sc-prod-1   10.0.0.61:80"]
        J3["sc-prod-2   10.0.0.62:80"]
        JA["each jail: nginx :80 -> apps :8001-8005<br/>SAME numbers in every jail"]
    end
    FP --> J2
    FP -.->|idle| J3
    FS --> J1
    J1 --> JA
    style ours fill:#dbeafe,stroke:#1e40af,color:#000
    style OTHER fill:#fecaca,stroke:#b91c1c,color:#000
    style jails fill:#bbf7d0,stroke:#15803d,color:#000
#+end_src

*Three tiers, and the number says which*: =dev(9000+)=,
=team(9100+)= and =protected(9200+)=. The port encodes whether an environment
can promote, the same way a label's name encodes its kind
(§Labels as state transitions).

| range        | tier          | what                                | allocated by        | can collide? |
|--------------+---------------+-------------------------------------+---------------------+--------------|
| *9000–9099*  | *dev*         | worktree blocks, =9000 + 10n=, n ≤ 9 | =ports.sh=, =ports.tsv= | *yes* — shared namespace |
| *9100–9199*  | *team*        | =env/<team>-<app>= — *cannot promote* | IDP checkout        | *yes*        |
| *9200*       | *protected*   | *staging*                            | =targets/node/deploy.sh= | *yes*   |
| *9210*       | *protected*   | *production — blue replica*          | =targets/node/deploy.sh= | *yes*   |
| *9220*       | *protected*   | *production — green replica*         | =targets/node/deploy.sh= | *yes*   |
| *9230*       | *protected*   | *production front* — proxies to the live colour | =targets/node/front.js= | *yes* |
| 9240–9299    | protected     | reserved                             | —                   | yes          |
| 7000–8999    | *not ours*    | dump1090 and friends                 | —                   | *avoid*      |
| =:80=, =8001–8005= | —       | inside every jail                    | fixed, identical everywhere | *no* |

*Amended 2026-09-13, in the direction of the observation.* This table read
=9200 front router → production= / =9201 front router → staging=, which the
estate contradicted: nothing ever bound 9201, and blue/green had no addresses
at all, so the two production replicas could not be spoken about. The protected
tier now uses the same ten-port blocks as every other tier — block 20 staging,
21 blue, 22 green, 23 the front — which is what =targets/node/deploy.sh= and
the gates actually implement.

Recorded rather than quietly corrected because *=spec.org= governs*: the code
was right and the governing document was wrong for several hours, and both
=CLAUDE.md= and =envs= were corrected before this file was. A spec that lags
its implementation is the bug the verification contract exists to catch.

The middle tier is the one worth having a range for. =README.org= §Environments
already says *team environments are for developing a change; staging is the
path to production* — and that they can never promote. Giving them their own
hundred makes that a property of the address rather than a rule someone
remembers: *anything at 9100–9199 is a dead end by construction.*

So: 9000 disposable, 9100 real but terminal, 9200 the path to production. *The
ceiling at block 9 is the boundary between dev and team*, which is why
=ports.sh= refuses block 10 rather than letting a worktree land in a tier that
means something else.

*** Why only the first two rows need a registry

A jail has its own IP, so =10.0.0.60:8001=, =10.0.0.61:8001= and
=10.0.0.62:8001= coexist — verified in hydra's socket table. Ports are free
where the namespace is not shared, which is why *the deployed config is
byte-identical across staging and production* and why the port-block scheme
exists only for worktrees, which do share hydra's namespace.

*** Two ceilings, both real

- *Ten worktrees.* Block 10 starts at 9100, which is the team tier — a dev
  block would be squatting an address that claims to be a team environment.
  =ports.sh alloc= refuses block 10 rather than crossing the boundary.
- *pf.* 10000+ is not passed from the LAN, so a block there is loopback-only
  and looks, from another host, exactly like nothing listening. The rule could
  be widened — we are root — but ten worktrees is enough and an open range with
  nothing behind it is surface for no benefit.

** Port allocation on a shared host

hydra runs other things. =8080= is =dump1090=, and =7500=, =7510=, =7680=,
=7681=, =7747=, =8081=, =8083= and =9630= belong to other services — so
"pick 8080" collides with a live ADS-B receiver. The project therefore owns a
*declared range* rather than convenient-looking numbers.

| range        | what                                    | who allocates      |
|--------------+-----------------------------------------+--------------------|
| *9000–9099*  | dev worktree blocks, =9000 + 10n=, n ≤ 9 | =ports.sh alloc=, via =ports.tsv= |
| *9100–9109*  | environment front routers: 9100 production, 9101 staging, 9102+ team | by hand, here |
| 9110–9199    | reserved for this project               | —                  |
| 7000–8999    | *other services* — do not use           | not ours           |

*Ten worktrees is the ceiling*, and =ports.sh= refuses block 10 rather than
silently walking into 9100 and taking the production router's port.

** Jail-internal ports do not consume this range

Inside every jail: nginx on =80=, apps on =8001–8005=. *The same numbers in
every environment*, because each jail has its own IP — verified in the host's
socket table, which shows =10.0.0.60:8001=, =10.0.0.61:8001= and
=10.0.0.62:8001= side by side.

That is the whole reason the deployed config is byte-identical across staging
and production (§bastille target): *network namespaces make ports free, and a
registry is only needed where a namespace is shared.* Worktrees share hydra's,
so they need one; jails do not, so they do not.

** Reaching a block from another host

Ports bind to =127.0.0.1= by default; =BIND=0.0.0.0= opts a block into the LAN.
That is necessary but *not sufficient* on hydra, where =pf= is enabled and
passes =192.168.86.0/24= only to ssh, 2812, 3000, =7000:7699= and =8000:9999=
[E, 2026-09-13]. *The default base of 10000 falls outside every open range*, so
a correctly-bound block is still refused — and the refusal is immediate, which
reads exactly like nothing listening.

=PORT_BASE= therefore selects the base: =PORT_BASE=9000= puts block =n= at
=9000 + 10n=, inside the range =pf= already allows, with no firewall change.
The blocks are otherwise identical.

: PORT_BASE=9000 ./change/ports.sh alloc
: BIND=0.0.0.0  ./change/ports.sh run-apps      # -> router on 9000 + 10n

Observed: blocks 1 and 2 serving on =:9010= and =:9020=, each reporting its own
build sha, =gates/e2e.sh= green against both over the LAN address.

** Exposure — what may cross the LAN boundary

Every port tier above says what an environment *is*. None of them says who may
*reach* it, and the two are not the same question. A public tunnel does not
change an environment's tier, its port, or its guards; it changes the set of
people who can operate the controls, and nothing in the tier model notices.

*** The control plane is not a web page

The dashboard reads like a status board and is not one. It carries
unauthenticated write endpoints:

| endpoint | effect |
|----------+--------|
| =POST /api/estate/freeze/{on,off}= | declares or lifts an estate freeze — halts EVERY deployment |
| =POST /api/estate/emergency/{on,off}= | declares or lifts an estate emergency |
| =POST /api/schedule/<pr>/clear= | cancels a booked window |

There is no authentication in front of them. On a LAN that is a deliberate
trade: the boundary IS the authentication, and `freeze` is designed to be
reachable in a hurry by whoever is in the room. Publish the same origin and the
trade silently inverts — the control that stops all deployment becomes an
unauthenticated POST from anywhere.

*The rule:* an origin that can change estate state must not be tunnelled without
an authenticating proxy in front of it. A read-only view may be; the same
process serving both is not a read-only view.

*** A tunnel is not obscurity

Three assumptions that do not hold, each of which has to be stated because each
sounds true:

- /Nobody knows the hostname./ Every hostname fronted by a CA-issued certificate
  appears in Certificate Transparency logs within minutes, and CT is crawled
  continuously. A tunnel hostname is public the moment it is created.
- /It is only reachable over the tunnel./ That is the point — the tunnel is
  reachable from the whole internet, which is strictly more exposure than the
  LAN, not less.
- /The catch-all 404 protects the rest./ It scopes the tunnel to declared
  hostnames. It says nothing about who may use a declared one.

*** The precedent already on this host

The existing =cloudflared= ingress on hydra gets this right and says so in its
own comments: the PTY WebSocket on 7681 is *never* tunnelled — "PTY access
requires LAN connection only" — and the demo hostname serves the static client
page *only*, with the socket it talks to deliberately left off the tunnel. The
rule there is the rule here: publish the view, never the actuator.

The telemetry beacon is the counter-example that shows the line. It is
tunnelled, and it is safe to tunnel, because it accepts events and controls
nothing. Exposure is decided by what an endpoint can *do*, not by what tier it
sits in.

*** The freeze has no declarer, and that is the actual defect

Exposure is the occasion for this finding, not its cause. The estate toggle
writes:

: `freeze` turned **on** from the IDP dashboard at <timestamp>.

No actor and no reason, posted under the repo's own token — so every freeze is
attributed to the same identity whoever pressed the button. That is already
true on a LAN.

It matters because a freeze is legitimately powerful: anyone with change
authority may halt the estate, and the control is deliberately easy to reach in
a hurry. The power is not the problem. The problem is that a =freeze= is a
property of the WORLD (see §Nomenclature) and a world-property with no recorded
declarer leaves the post-implementation review unable to answer /why did we stop
shipping/. An authorised halt and a hostile one produce byte-identical records.

Tunnelling the origin does not create this defect; it removes the only thing
compensating for it, which is that the set of people who could press the button
was the set of people in the building.

*The rule:* a control that changes estate state records WHO invoked it and WHY,
as part of the same write that changes the state. If the identity cannot be
established, the control refuses — an unattributable freeze is not a safer
freeze, it is an unauditable one.

*** What a rebuild must record

An environment registry that names tier and port but not *audience* cannot
express any of this — it will report an estate as =protected= while the whole
internet can freeze it. The registry needs a third axis: who may reach it, and
whether reaching it can change anything.


** Registry

=ports.tsv= is the CMDB for worktrees. =make port-alloc= appends a row for the current worktree path and writes =.env.ports= in the worktree. =make port-free= removes the row.

*Example only — deliberately not a =:tangle= target.* =ports.tsv= is mutable
state that =ports.sh alloc/free= writes to; tangling it would clobber live
allocations on every =org-babel-tangle-file=. =ports.sh alloc= creates the file
with its header on first use. [E — observed: a re-tangle mid-session restored a
stale row and displaced two worktrees by one block each.]

#+begin_example
block	worktree	branch	allocated_at
0	/path/to/standard-change	main	2026-09-12T00:00:00Z
#+end_example

** nginx template

The template is rendered once per worktree from =router/routes.json= and =.env.ports=. Each route prefix proxies to the owning app's port. A worktree that has not built an app falls back to the stable app on block 0, which is the local equivalent of a staging slot that deploys only the changed group.

#+begin_src nginx
# reference implementation of router/nginx.conf.tmpl -- that file is the source
# rendered per worktree: BLOCK, and per app PORT_<APP> (falls back to block-0 port)
worker_processes 1;
events { worker_connections 64; }
http {
  server {
    listen ${NGINX_PORT};
    location = /            { proxy_pass http://127.0.0.1:${PORT_CORE}; }
    location /login         { proxy_pass http://127.0.0.1:${PORT_CORE}; }
    location /account       { proxy_pass http://127.0.0.1:${PORT_CORE}; }
    location /cart          { proxy_pass http://127.0.0.1:${PORT_CORE}; }
    location /search        { proxy_pass http://127.0.0.1:${PORT_PLP}; }
    location /c/            { proxy_pass http://127.0.0.1:${PORT_PLP}; }
    location /p/            { proxy_pass http://127.0.0.1:${PORT_PDP}; }
    location /checkout      { proxy_pass http://127.0.0.1:${PORT_CHECKOUT}; }
    location /api/          { proxy_pass http://127.0.0.1:${PORT_MOCK}; }
  }
}
#+end_src

The =location= lines are generated from =routes.json=; the block above is the expected output, not the source. A gate (=gates/lint.sh=) fails when the rendered file and =routes.json= disagree.

** Cloud mirror

A staging slot =staging-<group>-<n>= is the same topology: one router in front of four app upstreams. The changed group points at the PR build; the other three point at the current production build. The Cloudflare target implements the router as a Worker with a service binding per app; the GitHub target implements it as a GitHub Environment per slot whose deployment payload records the four upstream SHAs (Pages serves the static builds; the router is simulated by =routes.json= plus per-app base paths).

* Labels

Supersedes the per-group staging-slot model sketched in §Router and ports
§Cloud mirror: *staging is a single environment, held by one PR at a time*, not
=n= slots per group. Which apps deploy is decided by the =app:*= labels on the
PR that holds it. Slots may return later as an optimization; they are not in
0.2. [P]

| Label                  | Set by      | Removed by        | Meaning                                                      |
|------------------------+-------------+-------------------+--------------------------------------------------------------|
| =app:<group>=          | labeller    | labeller          | The diff touches =apps/<group>/=. *Owned* by the labeller     |
| =change:standard=      | labeller    | labeller          | Diff limited to =apps/**=                                     |
| =change:normal=        | labeller    | labeller          | Diff touches =router/=, =gates/=, =change/=, =targets/=, =.github/= |
| =change:emergency=     | human       | human             | Authorizes the staging bypass. The only such authorization    |
| =change:requested=     | *human*     | release / close   | *Start my change request.* The only label a person adds to set a change going |
| =change:scheduled=     | scheduler   | activation        | A window is booked on the change schedule                    |
| =deploy:staging=       | workflow    | the action ends   | *Action in flight:* deploying to staging right now           |
| =staging:passed=       | workflow    | push to PR        | Authorizing e2e passed *on the current head SHA*             |
| =staging:failed=       | workflow    | push to PR        | Staging deploy or e2e failed; fix and re-request             |
| =deploy:production=    | workflow    | the action ends   | *Action in flight:* deploying to production right now        |
| =production:healthy=   | workflow    | release           | Post-deploy convergence check passed                         |
| =deployed:production=  | workflow    | —                 | Set after the PIR comment is posted                          |
| =blocked:queue=        | queue guard | queue guard       | Another open PR holds =deploy:staging=                        |
| =blocked:lock=         | preflight   | preflight         | Another deployment holds the deployment lock                 |
| =blocked:freeze=       | preflight   | preflight         | A freeze or emergency event covers the requested window      |

*Ownership matters.* The labeller runs with =sync-labels: true=, so it both adds
and removes =app:*= — a PR that stops touching =apps/pdp/= loses =app:pdp= and
stops deploying it. A human hand-adding =app:*= is overwritten on the next
push; this is intended, and it is why the =app:*= set can be trusted as the
build manifest.

*=labeler:skip= is the declared exception.* It stops the labeller entirely, so
=app:*= becomes hand-maintained — for forcing a build across apps the diff does
not touch. State the cost plainly, because it is the one the model cares about:
§Domain model says the manifest is *derived, never asserted*, and that if it can
be asserted "it is not a manifest — it is a wish, and the build ships whatever
someone typed". =labeler:skip= makes that true on purpose.

So it is a *declared deviation in label form*: visible on the PR, carried into
the PIR, and surfaced by =./status=. It does not disable any guard — the gates,
the berth and guards 4b and 5 are unaffected — it only moves the manifest from
derived to asserted, and says so where a reviewer can see it.

*=staging:passed= is bound to a SHA.* Any push to the PR removes it (and
=staging:failed=). A pass observed on an earlier commit authorizes nothing.

** Labeller

#+begin_src yaml
# reference implementation of .github/labeler.yml -- that file is the source
app:core:
  - changed-files:
      - any-glob-to-any-file: apps/core/**
app:plp:
  - changed-files:
      - any-glob-to-any-file: apps/plp/**
app:pdp:
  - changed-files:
      - any-glob-to-any-file: apps/pdp/**
app:checkout:
  - changed-files:
      - any-glob-to-any-file: apps/checkout/**
app:mock:
  - changed-files:
      - any-glob-to-any-file: apps/mock/**
change:normal:
  - changed-files:
      - any-glob-to-any-file:
          - router/**
          - gates/**
          - change/**
          - targets/**
          - .github/**
#+end_src

#+begin_src yaml
# reference implementation of .github/workflows/labeller.yml -- that file is the source
name: labeller
on: [pull_request_target]
permissions: {contents: read, pull-requests: write}
jobs:
  label:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/labeler@v5
        # labeler:skip hands the manifest back to a human -- for forcing a build
        # across apps the diff does not touch. See the note below: this converts
        # a DERIVED artifact into an ASSERTED one, and the PR wears the label
        # saying so.
        if: "!contains(github.event.pull_request.labels.*.name, 'labeler:skip')"
        with:
          sync-labels: true          # the labeller OWNS app:* and change:{standard,normal}
      # change:standard is the COMPLEMENT of change:normal, which actions/labeler
      # cannot express -- it matches globs, it cannot say "matched no other rule".
      # The offline oracle modelled this as derived and the config never
      # implemented it, so PRs got app:* and no change:* at all. Third time the
      # model and the implementation have diverged; the implementation was wrong
      # here, and only a live run showed it.
      - name: classify standard vs normal
        if: "!contains(github.event.pull_request.labels.*.name, 'labeler:skip')"
        env: {GH_TOKEN: "${{ github.token }}"}
        run: |
          # Derive from the DIFF, not from the labels actions/labeler just
          # wrote. Reading them back in the same job races the API: the first
          # version computed has_app from `gh pr view` and saw zero, so it
          # applied nothing and exited 0 -- a silent no-op that looked like a
          # policy decision.
          R=${{ github.repository }}
          N=${{ github.event.number }}
          files=$(gh pr diff "$N" --repo "$R" --name-only)
          has_app=$(echo "$files" | grep -c '^apps/' || true)
          is_normal=$(echo "$files" | grep -cE '^(router|gates|change|targets)/|^\.github/' || true)
          echo "changed=$(echo "$files" | wc -l) apps=$has_app normal=$is_normal"
          if [ "$is_normal" -gt 0 ]; then
            gh pr edit "$N" --repo "$R" --add-label change:normal || true
            gh pr edit "$N" --repo "$R" --remove-label change:standard || true
          elif [ "$has_app" -gt 0 ]; then
            gh pr edit "$N" --repo "$R" --add-label change:standard || true
            gh pr edit "$N" --repo "$R" --remove-label change:normal || true
          fi

      - name: drop stale staging verdicts on a new push
        if: github.event.action == 'synchronize'
        env: {GH_TOKEN: "${{ github.token }}"}
        run: |
          gh pr edit "${{ github.event.number }}" \
            --remove-label staging:passed --remove-label staging:failed || true
#+end_src

The second step is the SHA binding: a new push invalidates any staging verdict,
so =deploy:production='s guard can never see a pass from an older commit.

* Gates

Three gates run on every PR for every =app:*= group. Each gate exits non-zero on any finding; there is no warning tier.

| Gate | Script           | Scope                                                                         |
|------+------------------+-------------------------------------------------------------------------------|
| lint | =gates/lint.sh=  | eslint per app; =routes.json= schema; rendered nginx matches =routes.json=      |
| test | =gates/test.sh=  | =node --test= per app                                                         |
| e2e  | =gates/e2e.sh=   | Playwright: the group's journeys plus every =tests/e2e/= journey that touches it |

The e2e gate runs against a worktree-style router inside the runner (block 0 ports, all four apps built from the PR SHA). The same gate reruns against the staging slot URL after =deploy-staging=; the second run is the one that authorizes production.

** Negative tests for the gates

Each gate script has a fixture under =gates/fixtures/<gate>/fail/= that the gate must reject and =gates/fixtures/<gate>/pass/= it must accept. =make gate-selftest= runs both and fails if either outcome is wrong. This is a separate workflow job, required before the gates' own results count.

* Change enablement

** Preflight (=gates/preflight.sh=)

Runs before any staging or production deployment.

1. Lock: read the =deployment-lock= GitHub Discussion (or issue #1) body. If it names a PR other than this one and the run is under 60 minutes old, exit 2 and set =blocked:lock=. A lock older than 60 minutes is stale; preflight clears it and records the clearance in the PIR of the stale run.
2. Freeze: query the change schedule for events in the next 30 minutes with title prefix =FREEZE:= or =EMERGENCY:=. Any match: exit 3, set =blocked:freeze=. Repo variable =EMERGENCY_CHANGE=1= has the same effect and is the fallback when the calendar is unreachable.
3. Concurrency: production workflows use =concurrency: production= with =cancel-in-progress: false=; staging workflows use =concurrency: staging-<group>-<n>=.

Exit codes: 0 proceed, 2 lock, 3 freeze, 4 calendar unreachable and no repo variable set (treated as blocked).

** Change schedule (=change/schedule.sh=)

- =schedule.sh block <pr> <group> <minutes>= creates a calendar event =CHANGE: standard-change#<pr> <group>= on the agentic account's calendar with the owner as attendee and notifications on. The event description holds the PR URL, the SHA, and the staging slot.
- =schedule.sh check <window>= returns the freeze/emergency events overlapping the window.
- =schedule.sh close <event-id> <result>= appends the result (pass/fail/cancelled) to the event description at the end of the run.

The skill wraps these three commands. Deployment workflows call =block= before deploying and =close= in an =always()= step.

** Deployment lock (=change/lock.sh=)

=acquire <pr> <target>= writes =pr, target, run-id, started_at= to the lock body; =release= empties it. Release runs in an =always()= step. The 60-minute stale rule in preflight covers a runner that dies without reaching =release=.

* Promotion pipeline

One PR at a time owns staging; passing there promotes it automatically; a
healthy production deploy merges it and releases the queue.

** State machine

#+begin_src mermaid :eval never-export
stateDiagram-v2
    [*] --> Open: PR opened
    Open --> Gated: labeller sets app:*, gate.yml green on head SHA
    Gated --> Queued: human adds deploy:staging
    Queued --> Open: G0..G3 fail -> label removed, blocked:* set
    Queued --> Staging: guards pass, staging lock acquired
    Staging --> Gated: e2e fails -> staging:failed, lock released
    Staging --> Promoted: authorizing e2e passes -> staging:passed@SHA
    Promoted --> Production: promote.yml adds deploy:production
    Gated --> Production: change:emergency + approval (bypasses staging)
    Production --> Unhealthy: health check fails -> rollback + PIR
    Unhealthy --> Gated: labels released
    Production --> Healthy: health check passes -> production:healthy
    Healthy --> Merged: auto-merge, or a human merges
    Merged --> [*]: release -> all deploy:* labels removed, queue freed
#+end_src

** The guards

Each is a hard precondition. None is skippable except where stated, and the
only thing that can state it is =change:emergency=.

| # | Guard                    | Checked by             | On failure                          | Emergency bypass |
|---+--------------------------+------------------------+-------------------------------------+------------------|
| 0 | Up to date with =main=   | =change/queue.sh=      | remove =deploy:staging=, comment    | yes              |
| 1 | Staging held by no other PR | =change/queue.sh=   | =blocked:queue=, label removed      | n/a (skips staging) |
| 2 | Gates green on head SHA  | =promote.yml=          | refuse to promote                   | *no*             |
| 3 | No lock, no freeze       | =gates/preflight.sh=   | =blocked:lock= / =blocked:freeze=   | freeze only      |
| 4 | =staging:passed@head SHA= | =deploy-production.yml= | refuse to deploy                   | yes, with approval |
| 4b | Head still contains main's tip | =main-moved.yml=, =deploy-production.yml=, =release.sh= | withdraw pass; hold the berth | yes |
| 5 | Production health check  | =gates/health.sh=      | roll back, PIR, release             | *no*             |

*Guard 2 and guard 5 have no bypass at all.* An emergency may skip the staging
/environment/; it may not ship code whose unit and lint gates are red, and it
may not leave an unhealthy production deploy standing.

*Guard 2 is therefore evaluated twice*, in =promote.yml= and again in
=deploy-production.yml=. This is not belt-and-braces: =promote.yml= is gated on
the =staging:passed= label, so the emergency path never reaches it. A single
evaluation there would have given guard 2 precisely the bypass this paragraph
denies. [E — the omission was real and shipped; see §Open items.] Note the TLA+
model had it right all along: =Promote= and =DeployAndMerge= both require
=gated[p]=. The workflow, not the model, had drifted.

** Guard 0 — up to date with main

A standard or normal change must contain the current tip of =main= before it
may take staging. This is not tidiness: staging and production deploy the PR
head, so a PR that is /behind/ main would deploy a tree missing whatever merged
since — silently reverting the previous deployment. The guard is the thing that
makes "we don't lose the previous deployment" true rather than hoped-for.

It also drives the queue. When the holder merges and releases the labels, every
other open PR is now behind =main= by exactly that merge, so the next claimant
must rebase first. The rebase is not busywork — it is the re-verification that
the two changes compose, and it happens before staging rather than after
production.

: gh pr view <pr> --json mergeStateStatus -q .mergeStateStatus

=BEHIND= fails the guard. =BLOCKED= and =DIRTY= (conflicts) also fail. =CLEAN=
and =UNSTABLE= pass — =UNSTABLE= means checks are still running, which guard 2
catches properly at promote time with SHA binding.

** Guard 1 — staging is a singleton

#+begin_src bash :shebang "#!/bin/sh"
# reference implementation of change/queue.sh -- that file is the source
# queue.sh claim <pr>   -- guards 0 and 1. Exit 0 claim, 5 queue busy, 6 behind main.
set -eu
pr="$2"
repo="${GH_REPO:-$GITHUB_REPOSITORY}"

emergency=$(gh pr view "$pr" --repo "$repo" --json labels \
  -q '[.labels[].name] | index("change:emergency") // empty')

# Guard 0: up to date with main (standard and normal changes only).
#
# CLASSIFIED, like guard 4b -- but at a different threshold, and the difference
# is the point:
#   guard 4b blocks on artifact|hotfix   -- your tree would REVERT what shipped
#   guard 0  blocks on pipeline and above -- that, PLUS: main changed how you
#            are verified, so your branch would pass under the old gates
#   inert    blocks neither               -- docs cannot affect either
#
# Before this, guard 0 blocked on ANY divergence, so a docs merge forced every
# open branch to rebase for nothing. Found by enabling Dependabot: weekly
# control-plane bumps would churn every branch, and it was worth asking which
# of that churn was earned.
if [ -z "$emergency" ]; then
  state=$(gh pr view "$pr" --repo "$repo" --json mergeStateStatus -q .mergeStateStatus)
  if [ "$state" = "BEHIND" ]; then
    base=$(gh pr view "$pr" --repo "$repo" --json baseRefOid -q .baseRefOid)
    class=$(./change/divergence.sh "$base" origin/main 2>/dev/null || echo artifact)
    if [ "$class" = "inert" ]; then
      gh pr comment "$pr" --repo "$repo" --body \
        "Behind \`main\`, but only by an \`inert\` change (docs or notes). Nothing you ship or are verified by has moved, so the berth is granted without a rebase."
      state=CLEAN
    else
      gh pr comment "$pr" --repo "$repo" --body \
        "Staging refused: \`main\` moved by a \`$class\` change. $( [ "$class" = pipeline ] && echo "Your branch would be verified by the OLD gates." || echo "Deploying this tree would revert what shipped." ) Rebase onto \`main\` and re-request."
    fi
  fi
  case "$state" in
    BEHIND|BLOCKED|DIRTY)
      gh pr edit "$pr" --repo "$repo" --remove-label deploy:staging
      gh pr comment "$pr" --repo "$repo" --body \
        "Staging refused: branch is \`$state\` relative to \`main\`. Rebase onto \`main\` and re-add \`deploy:staging\` — deploying a tree behind main would revert the previous deployment."
      exit 6 ;;
  esac
fi

# Guard 1: no other open PR holds staging
holder=$(gh pr list --repo "$repo" --state open --label deploy:staging \
  --json number -q "[.[].number] | map(select(. != $pr)) | first // empty")
if [ -n "$holder" ]; then
  gh pr edit "$pr" --repo "$repo" --add-label blocked:queue --remove-label deploy:staging
  gh pr comment "$pr" --repo "$repo" --body \
    "Staging is held by #$holder. Re-add \`deploy:staging\` once it merges; you will need to rebase onto \`main\` first."
  exit 5
fi

gh pr edit "$pr" --repo "$repo" --remove-label blocked:queue || true
echo "claimed"
#+end_src

The label query is the authority, not the workflow concurrency group:
=concurrency= serializes /runs/, but a PR that deployed and is sitting in
production verification still /holds/ staging with no run in flight. The label
is the lock; =concurrency: staging= is only the anti-collision on top of it.

** Guard 4b — why a request-time guard expires

*Amendment, 0.2 → 0.3. Scenario D4 in =scenarios.org= refuted 0.2 and forced
this; the spec tracks observed reality.*

Guard 0 is evaluated once, when staging is claimed. =staging:passed= is
invalidated when the /PR/ moves. Neither covers =main= moving underneath a PR
that has already passed staging and is holding =deploy:production=. That PR
would deploy a tree predating whatever just merged — silently reverting it,
with every gate green, because the gates were green on a SHA that no longer
composes with main.

The emergency path makes this the normal case rather than a rare race: an
emergency /is/ a merge to main that jumps the queue.

#+begin_src yaml
# reference implementation of .github/workflows/main-moved.yml -- that file is the source
name: main-moved
on:
  push:
    branches: [main]
permissions: {contents: read, pull-requests: write}
jobs:
  invalidate:
    runs-on: ubuntu-latest
    env: {GH_TOKEN: "${{ github.token }}"}
    steps:
      - uses: actions/checkout@v4
      - name: expire staging verdicts that main has outrun
        run: |
          for pr in $(gh pr list --state open --label staging:passed --json number -q '.[].number'); do
            state=$(gh pr view "$pr" --json mergeStateStatus -q .mergeStateStatus)
            case "$state" in
              BEHIND|BLOCKED|DIRTY)
                gh pr edit "$pr" --remove-label staging:passed \
                                 --remove-label deploy:production \
                                 --remove-label deploy:staging
                gh pr comment "$pr" --body "\`main\` moved (${{ github.sha }}). Your staging pass described a tree that no longer composes with main, so it has been withdrawn along with the staging claim. Rebase onto \`main\` and re-add \`deploy:staging\`."
                ;;
            esac
          done
#+end_src

Guard 4 additionally re-checks =mergeStateStatus= at production time, because a
guard evaluated at request time expires:

: gh pr view "$PR" --json mergeStateStatus -q .mergeStateStatus   # must not be BEHIND

*The general rule, worth more than the instance:* any guard whose subject can
change after it is checked must be re-checked at the moment it is relied on.
Guard 0 at claim time is a convenience that gives fast feedback; guard 4b is
the one that is load-bearing. Note the amendment also drops =deploy:staging=,
which frees the queue — an expired claimant must not keep holding it.

** One label starts a change; the rest are the process

A person raises a change request. *That is the whole of their part.*

: change:requested          <- the human act, and the only one

Everything after is the system acting or observing, and none of it is a thing
to type:

| emitted            | kind        | means                                |
|--------------------+-------------+--------------------------------------|
| =change:scheduled= | observation | a window was booked for you          |
| =deploy:staging=   | *action*    | the workflow is deploying, right now |
| =staging:passed=   | observation | the authorizing run passed on this SHA |
| =deploy:production=| *action*    | the workflow is deploying to production |
| =production:healthy= | observation | convergence was measured           |

*=deploy:*= are actions in flight, not requests.* They mark "the workflow is
doing this", the way =staging:in-progress= did, and they clear when the action
ends. Reading them as a request — "deploy me to staging" — is what made the
earlier vocabulary incoherent: it put a thing the /system does/ in the column
of things a /person asks for/, and a person adding =deploy:staging= by hand was
asserting that the guards had been re-checked at their window when nobody had
checked anything.

The ITIL shape says the same in its own words: you raise an RFC; change
enablement schedules it, deployment management executes it, and neither is
something the requester performs.

** The berth, and four ways to lose it

A change occupies one of four positions. Only the last two are at risk from
=main= moving, and they are at risk /differently/.

| # | Position    | Means                                       | Labels                             |
|---+-------------+---------------------------------------------+------------------------------------|
| 1 | =scheduled= | window booked, no claim on the path         | =CHANGE:= event; no =deploy:*=     |
| 2 | =berthed=   | holds the path to production, deploying     | =deploy:staging=, =staging:in-progress= |
| 3 | =verified=  | authorizing e2e passed on this head         | + =staging:passed=                 |
| 4 | =cleared=   | promoted, about to ship                     | + =deploy:production=              |

=scheduled= is a record and nothing blocks on it. =berthed= is the scarce
resource. *Being verified is not being safe*: positions 3 and 4 are precisely
where a change looks finished and can still be wrong, because everything about
them was established against a =main= that has since moved.

** Guard 4b — classify the divergence, do not collapse it

=change/divergence.sh= classifies what landed under a berthed change. The
distinction is the whole guard: /"you are behind main"/ and /"you are about to
ship an image missing a hotfix"/ are different facts, and treating them alike
fails in both directions — a README merge forcing a full re-verify teaches
people the guard is noise, and a hotfix merge not forcing one is scenario D4.

*=apps/**= is the deployable entity.* In a container world the deployable is an
image and =apps/<x>/= maps to one; in a monorepo the directory is the closest
honest approximation. So *any* change to =apps/**= landing on main while a
change is on the deploy path is a blocker — not "a change to the same app", but
any of them, because the monorepo ships them together.

=targets/**= is deliberately *not* artifact. It is the deployment /mechanism/:
changing =deploy.sh= changes how a thing ships, not what ships, so it demands
re-verification rather than a revert.

| Class      | What landed                                | Berth | Staging pass | Message                                    |
|------------+--------------------------------------------+-------+--------------+--------------------------------------------|
| =inert=    | docs, =.meta=, =scenarios=, =experiments=, =tla= | kept  | *kept*       | informational only                         |
| =pipeline= | =targets/=, =gates/=, =change/=, =.github/=, =Makefile= | kept  | kept, re-run gates | "how you are verified or shipped changed" |
| =artifact= | =apps/=, =router/=, =targets/=             | kept + extended | *withdrawn* | "rebase and re-run staging"       |
| =hotfix=   | any merge carrying =Change-Type: emergency= | kept + extended | *withdrawn* | "you would ship without the fix"    |

*** This granularity belongs to the model we support

The rule is *"any =apps/**= change blocks"*, which is right for Option B — the
monorepo with one berth, the thing this spec actually implements. It is
deliberately coarser than the truth, and the ADR options are what make it
finer:

| if we took...                    | the rule becomes                          |
|----------------------------------+-------------------------------------------|
| *B* (this spec)                  | any =apps/**= change blocks — one trunk, one deployable |
| *A2* split the deployable out    | only an *overlapping app* blocks; =pdp= and =plp= stop invalidating each other |
| *A* contracts + path per team    | nothing cross-app blocks; the class stops mattering |
| *A4* deploy main                 | guard 0 is unstateable, so the class only governs guard 4b |

So the coarseness is a *property of the monorepo*, not a defect in the
classifier — and it is the same coarseness §The finding that decides this ADR
measures as 10–20 validations per shipped change. Refining it without splitting
the trunk would be optimising a symptom.

*The berth is never taken away by someone else's merge.* This is the correction
scenario D4's first fix got wrong: withdrawing =deploy:staging= freed the queue,
which is right when a claimant goes stale on its own, and wrong when it was
invalidated by someone jumping ahead of it. A change that was preempted keeps
its position and gets an extension (=change/state.sh extend=) to rebase and
revalidate. Losing your berth because someone else had an emergency is a
punishment for being in the queue at the wrong moment.

Staleness and preemption are therefore separated:

- *self-inflicted* — you pushed, or your lease simply expired → release the
  berth, rejoin the queue.
- *preemption* — =artifact= or =hotfix= landed under you → keep the berth,
  =.revalidate = true=, deadline extended, and the pass must be re-earned.

The two messages a =verified= change can receive, verbatim, because the
difference is what the reader needs:

#+begin_example
[artifact] main moved and it touched what you ship.
  merged: apps/pdp/src/price.js
  Your staging pass described an image that is no longer what you would
  deploy. Berth held until 14:20Z. Rebase onto main and re-add deploy:staging.

[hotfix] an emergency landed under you.
  CHG-20260912-0007 (#312) shipped apps/checkout/src/session.js
  Deploying your current image would REVERT it. This is not a staleness
  warning. Berth held until 14:35Z; the pass is withdrawn until staging
  re-runs on a tree containing the fix.
#+end_example

An =inert= divergence prints nothing beyond a note on the PR. A change that is
behind =main= only on documentation is behind in a way that cannot affect the
artifact, and guard 0 at the /next/ claim will make it rebase anyway.

** Promotion — staging pass to production

#+begin_src yaml
# reference implementation of .github/workflows/promote.yml -- that file is the source
name: promote
on:
  pull_request:
    types: [labeled]
permissions: {contents: read, pull-requests: write, checks: read}
jobs:
  promote:
    if: github.event.label.name == 'staging:passed'
    runs-on: ubuntu-latest
    env: {GH_TOKEN: "${{ github.token }}", PR: "${{ github.event.pull_request.number }}"}
    steps:
      - uses: actions/checkout@v4
      # Guard 2: every gate green on THIS head SHA. No bypass, emergency included.
      - name: require gates green on head SHA
        run: |
          sha="${{ github.event.pull_request.head.sha }}"
          bad=$(gh api "repos/${{ github.repository }}/commits/$sha/check-runs" \
                 --jq '[.check_runs[] | select(.name|test("^(gate-selftest|lint|test|e2e)")) 
                        | select(.conclusion != "success")] | length')
          total=$(gh api "repos/${{ github.repository }}/commits/$sha/check-runs" \
                 --jq '[.check_runs[] | select(.name|test("^gate-selftest$"))] | length')
          [ "$bad" -eq 0 ] || { echo "::error::$bad gate(s) not green on $sha"; exit 1; }
          [ "$total" -ge 1 ] || { echo "::error::gate-selftest never ran on $sha; gate results are void"; exit 1; }
      - name: promote
        run: |
          gh pr edit "$PR" --add-label deploy:production
          gh pr comment "$PR" --body "Staging authorizing run passed on \`${{ github.event.pull_request.head.sha }}\`. Promoting to production."
#+end_src

Guard 2 checks =gate-selftest= ran /and/ is green before trusting the other
three — a gate suite whose self-test never ran produces no verdict, so its
green is void rather than merely unverified (§Verification contract).

** Emergency — the only bypass

=change:emergency= is a human label and the sole authorization to reach
production without =staging:passed=. It does not bypass guard 2 or guard 5.

#+begin_example
change:emergency + deploy:production
  → require: one approving review from a change authority
  → require: gates green on head SHA        (guard 2, no bypass)
  → preflight: lock only; a FREEZE event does not block an emergency
  → deploy production
  → health check                            (guard 5, no bypass)
  → PIR records: bypassed staging, why, who approved, and the merge-base
    divergence from main at deploy time
#+end_example

An emergency PR is exempt from guard 0, so it may be behind =main=. The PIR
therefore records the divergence explicitly: shipping a tree behind main is a
known, accepted regression risk, and naming it is the difference between an
emergency change and an accident.

Note the asymmetry with §Change enablement: an =EMERGENCY:= event on the change
schedule /blocks everyone else/, while the =change:emergency= label /exempts one
PR/. Same word, opposite direction; the calendar governs the estate, the label
governs a change.

** Health check and release

#+begin_src bash :shebang "#!/bin/sh"
# reference implementation of gates/health.sh -- that file is the source
# health.sh <base-url> <expected-sha> [samples] -- guard 5. Exit 0, or 7.
#
# Asserts CONVERGENCE, not liveness. Two defects the single-sample version had:
#
#   1. It made TWO requests per route -- one for the status code, one for the
#      header -- so during a rolling deploy the first could hit an old replica
#      and the second a new one, and the pair would pass while the estate was
#      mixed. Both facts must come from ONE response.
#   2. One sample cannot distinguish "converged" from "I happened to reach a
#      new replica". A fleet that is 10% migrated passes one sample 10% of the
#      time, which is not a bug you find by re-running.
#
# So: N samples per route, every one must report the expected build. This is
# still only evidence, not proof -- see spec.org, What completes a deployment.
set -eu
base="$1"; want="$2"; samples="${3:-${HEALTH_SAMPLES:-5}}"; rc=0
MODE="${HEALTH_MODE:-header}"   # header | manifest (static targets)

for app in $(jq -r '.[].app' router/routes.json); do
  path=$(jq -r --arg a "$app" '.[] | select(.app==$a) | .health' router/routes.json)
  seen=""; bad=0
  n=1
  while [ "$n" -le "$samples" ]; do
    # Cache-bust. A CDN with max-age=600 serves the OLD build for ten
    # minutes after deploy, and a convergence check that trusts it is
    # measuring the CDN, not the estate (spec.org, Targets).
    bust="$(date +%s)-$n"
    if [ "$MODE" = "manifest" ]; then
      # Static targets (GitHub Pages) cannot set response headers, so the
      # served build is published as a generated file instead.
      body=$(curl -sS --max-time 10 -H "Cache-Control: no-cache" \
               "$base/version.json?_cb=$bust" 2>/dev/null || echo "{}")
      code=$(curl -sS -o /dev/null --max-time 10 -H "Cache-Control: no-cache" \
               -w "%{http_code}" "$base/version.json?_cb=$bust" 2>/dev/null || echo 000)
      sha=$(printf '%s' "$body" | jq -r '.sha // "-"' 2>/dev/null || echo "-")
    else
      # one request, both facts
      resp=$(curl -sS -o /dev/null --max-time 10 -H "Cache-Control: no-cache" \
               -w '%{http_code} %header{x-build-sha}' "$base$path?_cb=$bust" \
               2>/dev/null || echo "000 -")
      code=${resp%% *}; sha=${resp##* }
    fi
    case "$seen" in *" $sha "*) : ;; *) seen="$seen $sha " ;; esac
    if [ "$code" != "200" ] || [ "$sha" != "$want" ]; then bad=$((bad+1)); fi
    n=$((n+1))
  done
  distinct=$(echo "$seen" | tr ' ' '\n' | grep -c . || true)
  if [ "$bad" -gt 0 ]; then
    echo "UNHEALTHY $app: $bad/$samples samples not serving $want (saw:$seen)"
    rc=7
  elif [ "$distinct" -gt 1 ]; then
    echo "UNCONVERGED $app: still serving more than one build (saw:$seen)"
    rc=7
  else
    echo "ok $app $path ($samples/$samples on $want)"
  fi
done
exit $rc
#+end_src

The SHA assertion is the load-bearing half. A 200 only proves /something/ is
serving; =x-build-sha= proves the thing we just deployed is serving. A health
check that cannot distinguish the new deploy from the old one passes happily
through a failed deploy, which is the silent failure this gate exists to catch.

On =rc=7=: roll back to the previous production SHA from the deployment record,
post the PIR with the failure, remove =deploy:production= and =deploy:staging=,
and release the lock. The queue frees even on failure — a broken deployment
must not also wedge the pipeline.

On success: =production:healthy=, PIR comment, =deployed:production=, then

#+begin_src bash :shebang "#!/bin/sh"
# reference implementation of change/release.sh -- that file is the source
# release.sh <pr> -- merge (if auto-merge enabled) and free the queue.
set -eu
pr="$1"; repo="${GH_REPO:-$GITHUB_REPOSITORY}"

# Guard 4b, at the MERGE. Not redundant with the activation check: with more
# than one deployment in flight, two changes can both pass guard 4b (trunk has
# not moved yet) and then merge in sequence -- the first moves trunk, the
# second lands a tree predating it. Found in sim/ at berths=2. Activation is
# not the last point of reliance; the merge is.
emergency=$(gh pr view "$pr" --repo "$repo" --json labels \
  -q '[.labels[].name] | index("change:emergency") // empty')
if [ -z "$emergency" ]; then
  state=$(gh pr view "$pr" --repo "$repo" --json mergeStateStatus -q .mergeStateStatus)
  if [ "$state" = "BEHIND" ] || [ "$state" = "DIRTY" ]; then
    base=$(gh pr view "$pr" --repo "$repo" --json baseRefOid -q .baseRefOid)
    class=$(./change/divergence.sh "$base" origin/main || true)
    case "$class" in
      artifact|hotfix)
        gh pr edit "$pr" --repo "$repo" --remove-label staging:passed \
                                        --remove-label deploy:production
        gh pr comment "$pr" --repo "$repo" --body \
          "Merge refused: \`main\` moved during your window and it was a \`$class\` change. Production is deployed but this tree predates what is now on main, so merging it would revert that. Rebase and re-run staging; the berth is held."
        exit 8 ;;
    esac
  fi
fi

if [ "${AUTO_MERGE:-1}" = "1" ]; then
  gh pr merge "$pr" --repo "$repo" --squash --delete-branch
else
  gh pr comment "$pr" --repo "$repo" --body "Production healthy. Ready to merge; the staging queue frees on merge."
fi
gh pr edit "$pr" --repo "$repo" \
  --remove-label deploy:staging --remove-label deploy:production \
  --remove-label staging:passed --remove-label production:healthy
./change/lock.sh release

# Announce ONCE, on the queue issue -- never fan out to every waiter.
#
# The previous version commented on each blocked:queue PR. At the several-
# hundred-PR scale that is one API call per waiter per release, and worse than
# the cost: the queue has no ordering, so telling everyone at once produces a
# thundering herd that all rebase and race for a berth exactly one of them can
# take. Announcing once turns the queue into something waiters read rather than
# something that pages them.
gh issue comment "${QUEUE_ISSUE:-1}" --repo "$repo" --body \
  "Staging freed by #$pr at $(date -u +%FT%TZ). Next claimant: rebase onto \`main\` first -- guard 0 will refuse a branch that is behind."
#+end_src

Repo variable =AUTO_MERGE=0= turns the last hop manual: production is deployed
and verified, and a human merges. Everything before the merge is identical, so
the variable changes who presses the button and nothing about what was checked.

* Workflows

** gate.yml — the change authority

The four check runs guard 2 looks for. Job names matter: =promote.yml= and
=deploy-production.yml= query =commits/{sha}/check-runs= and match
=^(gate-selftest|lint|test|e2e)=, so a rename here silently disarms guard 2.

#+begin_src yaml
# reference implementation of .github/workflows/gate.yml -- that file is the source
name: gate
on:
  pull_request:
    types: [opened, synchronize, reopened, labeled]
permissions: {contents: read}

jobs:
  # Runs FIRST and gates the rest: a suite whose self-test did not pass
  # produces no usable verdict (spec.org, Verification contract).
  gate-selftest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.11'}
      - run: pip install --quiet hypothesis
      - run: ./gates/docs-lint.py
      - run: ./gates/docs-lint.py --selftest
      - run: ./gates/labeller-test.py
      - run: $(command -v gmake make | head -1) -s audit-selftest
      - run: ./gates/simulate-gates.py --check
      - run: ./gates/pbt-pipeline.py --exhaustive
      - name: the model must still be able to fail
        run: |
          if GUARD_4B=0 ./gates/pbt-pipeline.py --exhaustive >/dev/null; then
            echo "::error::the model cannot reach scenario D4; it verifies nothing"; exit 1
          fi

  lint:
    needs: gate-selftest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: '22'}
      - run: ./router/generate.sh
      - run: for a in apps/*/; do node --check "$a/src/server.js"; jq -e . "$a/routes.json" >/dev/null; done
      - name: routes.json must be the source of the router
        run: git diff --exit-code router/routes.json || { echo "::error::router/routes.json is stale; regenerate it"; exit 1; }

  test:
    needs: gate-selftest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: '22'}
      - run: for a in apps/*/; do (cd "$a" && node --test); done

  e2e:
    needs: gate-selftest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: '22'}
      - name: bring up a block in the runner
        run: |
          ./router/generate.sh
          # A fresh registry: the committed ports.tsv claims block 0 for a
          # developer's path, so alloc would hand this runner block 1 and the
          # router would not be where the next step looked.
          echo "PORTS_REGISTRY=$RUNNER_TEMP/ports.tsv" >> "$GITHUB_ENV"
          PORTS_REGISTRY=$RUNNER_TEMP/ports.tsv ./change/ports.sh alloc
          PORTS_REGISTRY=$RUNNER_TEMP/ports.tsv ./change/ports.sh run-apps
          sleep 4
      - name: journeys through the router
        run: |
          . ./.env.ports
          echo "router on $BASE_PORT"
          ROUTER_URL="http://127.0.0.1:$BASE_PORT" ./gates/e2e.sh
      - if: always()
        run: PORTS_REGISTRY=$RUNNER_TEMP/ports.tsv ./change/ports.sh stop || true
#+end_src

** deploy-staging.yml skeleton

#+begin_src yaml
# reference implementation of .github/workflows/deploy-staging.yml -- that file is the source
name: deploy-staging
on:
  pull_request:
    types: [labeled]
jobs:
  deploy:
    if: github.event.label.name == 'deploy:staging'
    runs-on: ubuntu-latest
    concurrency: {group: staging, cancel-in-progress: false}
    environment: staging
    env: {GH_TOKEN: "${{ github.token }}", PR: "${{ github.event.pull_request.number }}"}
    steps:
      - uses: actions/checkout@v4
      - run: ./change/queue.sh claim "$PR"          # guards 0 and 1
      - run: ./gates/preflight.sh staging           # guard 3
      - run: ./change/lock.sh acquire "$PR" staging
      - run: gh pr edit "$PR" --add-label staging:in-progress
      - run: ./change/schedule.sh block "$PR" "$(./change/groups.sh "$PR")" 30
      - run: ./targets/${{ vars.DEPLOY_TARGET }}/deploy.sh staging "$(./change/groups.sh "$PR")"
      - name: authorizing e2e run
        run: ROUTER_URL="$(cat .slot-url)" ./gates/e2e.sh $(./change/groups.sh "$PR")
      - if: success()
        run: gh pr edit "$PR" --add-label staging:passed --remove-label staging:in-progress
      - if: failure()
        run: gh pr edit "$PR" --add-label staging:failed --remove-label staging:in-progress
      - if: always()
        run: ./change/schedule.sh close "$(cat .change-event-id)" "${{ job.status }}"
      - if: always()
        run: ./change/lock.sh release
#+end_src

=change/groups.sh <pr>= reads the PR's =app:*= labels and prints the deploy
groups — the labeller-owned set is the build manifest (§Labels).

Note what is /not/ released on the staging path: =deploy:staging= stays on the
PR through production. Staging is held until the change lands or fails out, so
the queue reflects "this PR owns the pipeline", not "a deploy job is running".

** deploy-production.yml skeleton

#+begin_src yaml
# reference implementation of .github/workflows/deploy-production.yml -- that file is the source
name: deploy-production
on:
  pull_request:
    types: [labeled]
permissions: {contents: write, pull-requests: write, deployments: write}
jobs:
  deploy:
    if: github.event.label.name == 'deploy:production'
    runs-on: ubuntu-latest
    concurrency: {group: production, cancel-in-progress: false}
    environment: production          # require reviewers here for change:normal
    env: {GH_TOKEN: "${{ github.token }}", PR: "${{ github.event.pull_request.number }}"}
    steps:
      - uses: actions/checkout@v4
      # Guard 2 AGAIN, here and not only in promote.yml. promote.yml is gated on
      # the staging:passed label, so the emergency path never reaches it; without
      # this step guard 2 has exactly the bypass the spec says it does not have.
      - name: require gates green on head SHA
        run: |
          sha="${{ github.event.pull_request.head.sha }}"
          runs=$(gh api "repos/${{ github.repository }}/commits/$sha/check-runs")
          bad=$(echo "$runs" | jq '[.check_runs[]
                 | select(.name|test("^(gate-selftest|lint|test|e2e)"))
                 | select(.conclusion != "success")] | length')
          self=$(echo "$runs" | jq '[.check_runs[]
                 | select(.name|test("^gate-selftest$"))
                 | select(.conclusion == "success")] | length')
          [ "$bad" -eq 0 ] || { echo "::error::$bad gate(s) not green on $sha"; exit 1; }
          [ "$self" -ge 1 ] || { echo "::error::gate-selftest did not pass on $sha; gate results are void"; exit 1; }
      # Guard 4: no production without a staging pass on THIS SHA, unless emergency.
      - name: require staging pass or emergency
        run: |
          labels=$(gh pr view "$PR" --json labels -q '[.labels[].name]')
          echo "$labels" | jq -e 'index("staging:passed")' >/dev/null && exit 0
          echo "$labels" | jq -e 'index("change:emergency")' >/dev/null || {
            echo "::error::no staging:passed on head SHA and not change:emergency"; exit 1; }
          gh api "repos/${{ github.repository }}/pulls/$PR/reviews" \
            --jq '[.[]|select(.state=="APPROVED")]|length' | grep -qv '^0$' || {
            echo "::error::change:emergency requires an approving review"; exit 1; }
      - run: ./gates/preflight.sh production
      - run: ./change/lock.sh acquire "$PR" production
      - run: ./targets/${{ vars.DEPLOY_TARGET }}/deploy.sh production "$(./change/groups.sh "$PR")"
      - name: health check
        run: ./gates/health.sh "${{ vars.PRODUCTION_URL }}" "${{ github.event.pull_request.head.sha }}"
      - if: failure()
        run: ./targets/${{ vars.DEPLOY_TARGET }}/rollback.sh production
      - if: always()
        run: ./change/pir.sh "$PR" "${{ job.status }}"
      - if: success()
        run: |
          gh pr edit "$PR" --add-label production:healthy --add-label deployed:production
          ./change/release.sh "$PR"
      - if: always()
        run: ./change/lock.sh release
#+end_src

** Targets

=vars.DEPLOY_TARGET= selects =github= or =cloudflare=. Both expose =deploy.sh <env> <groups...>=, =rollback.sh <env>=, and write =.slot-url=.

| Target     | Staging                                    | Production                       | Router                          |
|------------+--------------------------------------------+----------------------------------+---------------------------------|
| github     | Environment =staging=; Pages branch        | Environment =production=; Pages  | =routes.json= + base-path rewrite |
| cloudflare | Worker per app named =<group>-staging=     | Worker per app, production route | Router Worker, service bindings |

Each target must expose the served build. *A response header is not always
available*: GitHub Pages serves static files through a fixed CDN header set and
*cannot set custom response headers* [E, verified 2026-09-13 against
=pages.github.com=], so =x-build-sha= is unimplementable there. Two modes:

| target                | how the served build is exposed          | guard 5 mode |
|-----------------------+------------------------------------------+--------------|
| cloudflare (Worker)   | =x-build-sha= response header            | =header=     |
| github (Pages)        | a generated =/version.json= =({"sha":…})= | =manifest=   |

Pages also sends =cache-control: max-age=600= [E, observed =age: 284= on a
live response], so *"deployed" and "served" can be ten minutes apart*. Guard 5
must bust the cache or wait out the TTL; a convergence check that trusts a CDN
is measuring the CDN. This is the clearest case of §Domain model's C2 warning
arriving in a new place: the artifact you verify must be the artifact being
served, and a cache is another way for those to differ.

* Checklists

Each checklist is a =.github/PULL_REQUEST_TEMPLATE= section and a =pir.sh= field. Unchecked required items block the corresponding label from taking effect (=gates/preflight.sh= reads the PR body).

** Standard change (before =deploy:<group>=)

- [ ] =app:*= labels match the intended groups
- [ ] Gate self-test green
- [ ] lint, test, e2e green on head SHA
- [ ] Change window length stated (default 30 minutes)

** Production (before =deploy:production=)

- [ ] =deployed:staging-<group>-<n>= present for every =app:*= group
- [ ] Staging e2e run is on the head SHA (not a prior push)
- [ ] No =blocked:*= label
- [ ] For =change:normal=: one approving review from a listed change authority
- [ ] Rollback SHA recorded in PR body (=pir.sh= fills this from the production deployment record)

** Post-implementation review (generated)

- Gate durations per app
- Staging slot and window event ids
- Production deployment record id and previous SHA
- Any stale-lock clearance or freeze wait encountered
- Journeys run against production and their results

* Local development

*The =Makefile= is hand-maintained, not tangled.* It changes often and is
edited directly, and two changes were silently reverted by a tangle before that
was recognised. Shown here for orientation; =Makefile= is the source.

#+begin_example
# Bare metal by choice: this is a CI/CD pipeline simulator and FreeBSD is a
# poor host for docker/podman. Apps are node processes on a port block.
# On hydra use gmake; system make is BSD make.
APPS := $(notdir $(wildcard apps/*))

.PHONY: help env env-check run dev router stop test lint gate gate-selftest \
        audit audit-selftest docs pbt pbt-random simulate simulate-gates \
        port-alloc port-free ports clean

help:
	@echo "env / env-check   .env from .env.template (warns if stale)"
	@echo "port-alloc        claim a port block for this worktree"
	@echo "run               start every app via apps/*/Makefile"
	@echo "dev               run + router"
	@echo "gate              lint, test, e2e"

# .env is generated and gitignored; .env.template is tracked.
# Compose, do not clobber. change/env.sh preserves local overrides and reports
# what changed; `cat template ports > .env` silently discarded them.
.env: .env.template
	@./change/env.sh

env: .env

env-check:
	@if [ ! -f .env ]; then \
	  echo "WARNING: no .env — run 'gmake env'"; exit 1; \
	elif [ .env.template -nt .env ]; then \
	  echo "WARNING: .env.template is newer than .env — run 'gmake env'"; exit 1; \
	else echo ".env current"; fi

# Delegates to each app's own Makefile, so every app has the same targets.
run: env-check
	@for a in $(APPS); do \
	  $(MAKE) -C apps/$$a dev & \
	done; \
	echo "started: $(APPS)"

router: env-check
	@. ./.env && BASE_PORT=$$BASE_PORT BLOCK=$$BLOCK node router/server.js

dev: ; @./change/ports.sh run-apps
stop: ; @./change/ports.sh stop

test:  ; @for a in $(APPS); do $(MAKE) -s -C apps/$$a test || exit 1; done
lint:  ; @./router/generate.sh >/dev/null && \
	  for a in $(APPS); do $(MAKE) -s -C apps/$$a lint || exit 1; done && \
	  ./gates/labeller-test.py

gate: lint test ; @./gates/e2e.sh $(app)
gate-selftest: docs-selftest
	@./gates/labeller-test.py && ./tla/check.sh && $(MAKE) -s audit-selftest

# The documents gate and its own negative test. A gate that cannot fail
# verifies nothing, so the malformed fixture must be rejected.
docs-selftest:
	@./gates/docs-lint.py
	@./gates/docs-lint.py --selftest
audit-selftest:
	@./gates/audit-controls.py --repo o/r --fixture gates/fixtures/audit/pass >/dev/null \
	  || { echo "audit rejects a compliant fixture"; exit 1; }
	@./gates/audit-controls.py --repo o/r --fixture gates/fixtures/audit/fail >/dev/null \
	  && { echo "audit passes a non-compliant fixture"; exit 1; } || true
	@echo "audit-controls: both directions confirmed"
	@./gates/simulate-gates.py --check
	@./gates/pbt-pipeline.py --exhaustive >/dev/null \
	  || { echo "pbt: clean model reports a violation"; exit 1; }
	@GUARD_4B=0 ./gates/pbt-pipeline.py --exhaustive >/dev/null \
	  && { echo "pbt: model cannot find scenario D4; it verifies nothing"; exit 1; } || true
	@echo "pbt-pipeline: both directions confirmed"
audit:           ; @./gates/audit-controls.py
docs:            ; @./gates/docs-lint.py
# Forge through batch emacs, so it works whether or not emacs is running.
forge-list:      ; @emacs --batch -l standard-change.el -f standard-change-forge-list
forge-pull:      ; @emacs --batch -l standard-change.el -f standard-change-forge-pull
forge:           forge-pull forge-list
forge-check:     ; @emacs --batch -l standard-change.el -f standard-change-forge-check

pbt:             ; @./gates/pbt-pipeline.py --exhaustive
pbt-random:      ; @./gates/pbt-pipeline.py
simulate-gates:  ; @./gates/simulate-gates.py
simulate:        ; @./change/simulate.sh $(app)
port-alloc:      ; @./change/ports.sh alloc
port-free:       ; @./change/ports.sh free
ports:           ; @./change/ports.sh list
clean:           ; @rm -rf .run .env .env.ports
#+end_example

A worktree is a sibling of the main checkout. =make port-alloc= is the first command in a new worktree; =make port-free= is the last.

* Governance tuple and support matrix

** The tuple

Every claim in this repo lives at a coordinate:

: (runtime, platform, router, browser)

- *runtime* — the Node that runs the apps and =node --test=.
- *platform* — the OS the gate ran on.
- *router* — how the four apps are fronted: =nginx= locally, a Worker or
  base-path rewrite in the cloud.
- *browser* — what Playwright drives.

A cell is *supported* only when a gate was /watched/ to pass on it, with
versions recorded in §Host survey of record. Anything else is [H].

** Matrix

| Cell                              | lint | test | e2e | Status                                             |
|-----------------------------------+------+------+-----+----------------------------------------------------|
| ubuntu-latest / CI                | [H]  | [H]  | [H] | GitHub stopped scheduling jobs 2026-09-13 (#34); every run reports failure with 0 steps |
| FreeBSD 14.4 amd64 / hydra        | [E]  | [E]  | [E] | *router = node, not nginx. browser NOT exercised.* |
| macOS-arm64 / mini                | [E]  | [E]  | [E] | *router = node. browser NOT exercised.* 2026-09-14, below |

The claim in one line, revised 2026-09-13: *one cell is supported, and only on
part of its tuple.* On hydra, =gmake lint=, =gmake test= and =gates/e2e.sh=
against staging were each watched to pass — 27 e2e checks against build
=2567015= at 22:25Z, Node v24.14.1, GNU Make 4.4.1, FreeBSD 14.4-RELEASE-p6.

*Second cell, 2026-09-14, macOS.* =gmake lint=, =gmake test=, =gmake
gate-selftest= and =ROUTER_URL=http://127.0.0.1:9000 gmake gate app=plp= were
each watched to pass on the mini — 27 e2e checks and the smoke walk against
build =fb4cde5=, Node v26.2.0, GNU Make 4.4.1, Darwin 24.1.0, Python 3.11.10
with Hypothesis 6.111.0. Same partial tuple as hydra: node router, no browser.
Two things the run needed that the Makefile does not say: =python3= must be a
3.11 with Hypothesis on =PATH= (the default 3.14 has none, and =pbt= then
reports "clean model reports a violation" for an =ImportError=), and
=gates/e2e.sh= still defaults =ROUTER_URL= to =:10000=, the pre-correction
block, so =gmake gate= exits 7 from curl unless the URL is given. Both are
issues, not spec.

*What that does NOT promote.* The tuple is (runtime, platform, router, browser),
and each cell is observed on two of the four:

- *router* — the router under test was =router/server.js=, the node router the
  port-block model uses. nginx is installed but =gmake router= has not been
  watched. A gate passing behind one router says nothing about the other.
- *browser* — =gates/e2e.sh= is curl-based. Playwright ships no FreeBSD browser
  build, so no cell has a browser observation at all, and =e2e= here means the
  journey assertions, not a rendered page.

The earlier text — "no cell is supported today; the repo has no code" — was
true when written and stopped being true without anyone editing it. That is the
failure mode this matrix exists to catch, so it is recorded rather than
quietly corrected.

** Known blockers on hydra (FreeBSD), from the survey

- *nginx* — *resolved 2026-09-13*. The =pkg= catalogue was stale; after
  =pkg update=, =nginx-1.30.4= installed cleanly [E]. =router/server.js= remains
  the default local router because it needs no root to run, but the nginx path
  in the spec is now testable rather than hypothetical.
- *=gitea-act_runner-0.4.0_4= is packaged* [E] — installing it prints, from the
  port itself: "FreeBSD does not support Docker. When configuring the runner,
  use 'host' as the label." That is the constraint stated by the software, not
  inferred: host mode only, so no =container:= or =services:= steps.
- *Playwright cannot run on FreeBSD at all* [E, refuted 2026-09-13]. The
  earlier hypothesis — skip the browser download and point =executablePath= at
  the system Chromium — *is wrong, and not for the reason assumed*.
  =playwright-core= throws =Error: Unsupported platform: freebsd= from
  =computeDefaultCacheDirectory= during *module initialisation*, before any
  config, =executablePath= or environment variable is read. There is nothing to
  configure around.

  Chromium itself is fine: =chrome --headless --dump-dom= against a live
  worktree router returns the page [E]. So the browser works and the *driver*
  refuses. Consequences: hydra's e2e cell is *unsupported*, the Playwright gate
  is *CI-only* (ubuntu-latest), and local end-to-end coverage comes from
  =gates/e2e.sh=, which needs only =curl= and =jq=. A CDP driver against the
  system Chromium would also work and is not currently worth building.
- *eslint is not installed* [E]; it is a project-local dev dependency, so this
  resolves on first =npm install= rather than needing a system package.
- *=make= is BSD make* [E]; GNU Make 4.4.1 is available as =gmake= [E]. The
  =Makefile= above is GNU-flavoured, so on hydra the target is =gmake=, not
  =make=. CI on ubuntu-latest uses =make=.
- *wrangler is not installed* [E]; the Cloudflare target is unexercised.

* Verification contract

- *Two gates, different things.* The code gate is =gates/{lint,test,e2e}.sh=.
  The documents gate is this spec: org blocks balance, mermaid renders, every
  internal reference names a section that exists, and =CLAUDE.md= asserts
  nothing absent here. It no longer checks tangle targets — there are none
  (§This spec is intent, not source). Neither gate substitutes for the other.
- *Ordering is load-bearing.* =gate-selftest= runs /before/ the gates' own
  results count. A gate whose negative fixture it fails to reject produces no
  usable verdict that run — its PASS is void, not merely suspect.
- *Pass criterion.* A gate passes when it exits 0 with zero findings. There is
  no warning tier.
- *Promotion.* A matrix cell moves from [H] to [E] only when a human watched
  all three gates pass on it and the exact versions were pasted into §Host
  survey of record. A green run on convenient infrastructure grants a claim
  nothing that was not observed on a real target cell.
- *The second e2e run authorizes production*, not the first. The in-runner run
  proves the build; the staging-slot run proves the deployment.

* Dependency updates

Only =github-actions=, and the reasoning is the same one §Extensions uses: do
not add machinery for a thing that does not exist.

| ecosystem       | present? | why not tracked                                    |
|-----------------+----------+----------------------------------------------------|
| =github-actions=| *14 pins across 4 actions* | tracked — they do go stale       |
| =npm=           | 5 manifests, *zero dependencies*, no lockfiles | nothing to update; the apps are stdlib-only by design |
| =pip=           | no manifest | =hypothesis= is installed ad hoc in =gate.yml=; declaring a =requirements.txt= purely to be scanned would be adding a manifest for the scanner's benefit |

A live run already justified the one we keep: =actions/labeler@v5= warned that
it targets the deprecated Node 20.

** A dependency bump is a normal change, and that is correct

A Dependabot PR touches =.github/=, so the labeller gives it =change:normal=,
which requires an approving review from a change authority (§Labels). *A bump
to the control plane is exactly the change that should not be automatic* — the
actions being updated are the ones that run the gates.

The config groups all actions into one PR on purpose. Four separate PRs would
each have to take the berth in turn, and the berth is a singleton (guard 1);
dependency bumps should not spend four windows doing one thing. That is
§The balance applied to robots rather than people.

** main is not protected, and cannot be here

*Amended 2026-09-14, in the direction of the observation.* The repo is now
public, and =branches/main/protection= returns 200: =strict: true=, no
required contexts, no reviewers; =rulesets= returns an empty list;
=allow_auto_merge= is =false=. So the title below is no longer true — =main=
*can* be protected here, and today it carries a rule that requires being up to
date with nothing required to pass. The rest of this section describes the
private-repo state it was written in and is kept as the record of why the
workflow-level guards exist. =gmake audit= now reads the rules rather than
reporting them =UNAVAILABLE= (5 ok, 3 findings, 3 absent, 0 unreadable).
=docs/pipeline-history.org= §Availability on this repo has the commands.

GitHub's own settings page says so: /"Your main branch isn't protected."/ It is
correct and there is no fix on this plan — =repos/{r}/rulesets= and
=branches/main/protection= both return 403 on a private repo (§Availability on
the development box). Enabling Dependabot and the Advanced Security toggles
does not change it; those are different features.

*What the simulation does about it.* The rules stay specified — the payload at
=.github/config/ruleset-main.json= is validated against what the audit reads,
and =gates/audit-controls.py= reports them =UNAVAILABLE= rather than =ABSENT=,
which is the distinction that whole tool exists for. What changes is that the
two rules with no workflow analogue stop being silent:

| ruleset rule           | preventable here? | substitute                      |
|------------------------+-------------------+---------------------------------|
| require a PR           | no                | *detect* a push with no merged PR |
| block force push       | no                | *detect* =forced: true= on the event |
| restrict deletion      | no                | nothing — the objects are already gone |
| require status checks  | no, at merge      | guard 2 still gates *production* |
| linear history         | no                | detect a merge commit           |

*Detection is not prevention and the difference matters*: by the time
=main-moved.yml= sees a force push, the objects it overwrote may already be
unreachable, which is exactly what the rule was protecting (§Controls audit —
"the recorded production SHA stays reachable"). The substitute makes the gap
*observable*, not closed.

#+begin_src yaml
# reference implementation of .github/workflows/trunk-audit.yml -- that file is the source
name: trunk-audit
on:
  push:
    branches: [main]
permissions: {contents: read, issues: write}
jobs:
  audit:
    runs-on: ubuntu-latest
    env: {GH_TOKEN: "${{ github.token }}"}
    steps:
      - name: a force push overwrote history
        if: github.event.forced
        run: |
          gh issue comment 1 --body "FORCE PUSH to main at $(date -u +%FT%TZ) by ${{ github.actor }}: before=${{ github.event.before }} after=${{ github.event.after }}. A ruleset would have blocked this; on this plan it can only be recorded, and the overwritten objects may already be unreachable."
          echo "::error::force push to main detected"
      - name: a commit arrived without a pull request
        if: "!github.event.forced"
        run: |
          for sha in $(echo '${{ toJson(github.event.commits.*.id) }}' | jq -r '.[]?'); do
            n=$(gh api "repos/${{ github.repository }}/commits/$sha/pulls" \
                  -H "Accept: application/vnd.github.groot-preview+json" --jq 'length' 2>/dev/null || echo 0)
            [ "$n" -gt 0 ] || {
              gh issue comment 1 --body "DIRECT PUSH to main: \`$sha\` by ${{ github.actor }} has no associated pull request. Every change is supposed to be a PR (spec.org, Vocabulary)."
              echo "::warning::$sha reached main without a PR"; }
          done
#+end_src

** A merge queue is most of this model, natively

GitHub's merge queue builds each queued PR *against the current tip of main* in
a temporary branch, runs the required checks on that combined state, and merges
only if it is green. That is guard 0 and the merge-time half of guard 4b, done
by the forge.

| our guard            | merge queue does it? | note                                    |
|----------------------+----------------------+-----------------------------------------|
| *0* up to date       | *yes, natively*      | the candidate IS built on current main  |
| *4b* at merge        | *yes, natively*      | checks re-run on the merge candidate    |
| *1* singleton berth  | partly               | the queue serialises, and can *batch*   |
| *2* gates green      | yes, at merge        | but merge-time only — see below         |
| *4* staging pass     | *no*                 | it merges; it does not deploy           |
| *5* production healthy | *no*               | nothing after the merge is its concern  |

And it is the *release train relocated to the merge point* —
=docs/release-strategies.org= reached that conclusion independently, and
measured what batching buys: *0.58 validations per shipped change against
13.63*, at the cost of latency. A merge queue is that amortisation, native.

*What it does not replace.* This pipeline deploys to production *before* the
merge (§What completes a deployment). A merge queue governs the merge, so it
cannot see guard 5 at all, and cannot close the production-ahead-of-main
divergence — which is the one that needs a rollback rather than a deploy. If
the ordering were inverted (merge first, deploy from main), a merge queue would
replace most of the model; with the current ordering it replaces the front half.

*Unavailable here*: a merge queue requires branch protection or a ruleset, both
403 on this plan (§main is not protected). =allow_auto_merge= is =false= and
=allow_update_branch= is =false= on this repo, so even the weaker native
helpers are off.

So: specified, understood, and not reachable — the same status as the ruleset
table, and worth recording at the same fidelity so an enterprise rollout starts
from "turn the queue on" rather than from "build a berth".

** Approval as a gate, and what an agent review is worth

*Not enabled.* Recorded because the design question is sharper than the switch.

=allow_auto_merge= is =false= on this repo, and requiring an approving review
needs a ruleset — 403 here (§main is not protected). So this is specification,
not configuration.

*** An approval is an observation label

By §Labels as state transitions, "approved" is =<subject>:<state>= — a claim
that someone looked. Which puts it in the forgeable column, and raises the
question the taxonomy always raises: *does the approver re-derive anything, or
only assert?*

| review by                       | re-derives? | worth as a gate                     |
|---------------------------------+-------------+-------------------------------------|
| a person who read the diff      | yes, weakly | judgement the gates cannot encode   |
| an agent that ran the gates     | *yes*       | *evidence* — but it duplicates gate.yml |
| an agent that says "looks good" | no          | *nothing* — an observation nobody made |
| the author, of their own change | no          | nothing, at any level of effort     |

*An agent review counts exactly insofar as it re-derived something the gates
did not.* An agent that re-runs =gate.yml= adds no information; an agent that
reads the change for what the gates /cannot/ check — is this reversible (A5),
does the rollback target still exist, does the PIR say what it should — is
supplying evidence nobody else is.

That is the honest brief for an agent reviewer here: *do not review the code
the gates already judge.* Review the things no gate asks.

*** The single-identity problem

This repo has two collaborators, =jwalsh= and =aygp-dr=, and in practice one
person behind both. So a required approval is satisfiable but not *meaningful*
— and =prevent_self_review= would deadlock every change, which is why
=.github/config/ruleset-main.json= sets it =false= with a note.

Worth stating rather than quietly configuring around: *a required approval on a
one-maintainer repo is theatre unless the approver is genuinely a second
party.* An agent is a second party only if it is a second /judgement/, not a
second click.

*** If it were enabled

: ruleset: pull_request.required_approving_review_count = 1
: repo:    allow_auto_merge = true

with =change:normal= already requiring it (§Labels) and =change:standard=
relying on the gates as the change authority — which is the existing split, and
the reason the split exists.

* Controls audit

Every control the spec claims is auditable as JSON, without a UI visit.
=gates/audit-controls.py= is four reads: rulesets, the workflow files,
environments, and repository variables.

** Three outcomes, never two

The audit distinguishes =ok= / =FINDING= / =ABSENT= / =UNAVAILABLE=, and the
last is the one that matters. *An audit that reports "no findings" because it
could not read anything is worse than no audit* — it launders an unknown as a
pass, which is the failure mode §Verification contract exists to prevent.
Unreadable controls exit 2, distinct from findings (1) and clean (0).

** Ruleset rules to spec controls

| Ruleset rule                   | Control it enforces                              | Audit check                                      |
|--------------------------------+--------------------------------------------------+--------------------------------------------------|
| Require a pull request         | every change is a PR with =app:*=; no direct push | =rules[].type == "pull_request"=; approvals >= 1  |
| Require status checks          | the gates are the change authority                | contexts ⊇ {=gate-selftest=, =lint=, =test=, =e2e=} |
| — its =strict= flag            | guard 0, *at merge only* — see below              | =strict_required_status_checks_policy == true=    |
| Require deployments to succeed | staging before production                         | =required_deployment_environments= lists =staging= |
| Require linear history         | one logical step per commit; single-parent rollback | =required_linear_history=                       |
| Block force push / deletion    | the recorded production SHA stays reachable       | =non_fast_forward=, =deletion=                    |
| Bypass list                    | who may skip the change authority                 | empty, or only the named emergency actor          |
| Enforcement status             | rules exist but are not applied                   | =enforcement == "active"=; =evaluate= is a finding |
| Target branches                | rules cover the production branch                 | =~DEFAULT_BRANCH= or =refs/heads/main=            |

Out of scope, and ignored deliberately: code scanning, code quality, coverage
thresholds, Copilot review. A project may enable them; this model makes no
claim about them, and an audit that scored them would be scoring someone else's
spec.

** =strict= does not protect production [E]

Their natural reading is that =strict_required_status_checks_policy= covers the
"staging run is on the head SHA" checklist item. It does not, and the reason is
specific to this pipeline: *production deploys before the merge.* In
=deploy-production.yml= the health check runs at the deploy step and
=change/release.sh= merges afterwards. So =strict= gates a merge that happens
/after/ production has already taken the change.

=strict= is therefore a backstop on =main='s history, not a guard on the
estate. It is a partial, native implementation of *guard 0* — and only at merge
time. *Guard 4b remains load-bearing and is not replaceable by any ruleset
rule*, because the window it closes opens after every ruleset check has passed.

** The per-group limitation dissolved at 0.3

=required_deployments= is static per ruleset and cannot express "only the slot
for the changed group". That was a real limitation against the 0.1 model's
=staging-<group>-<n>= slots. At 0.3 staging is a *singleton* (§Labels), so
=required_deployment_environments: ["staging"]= expresses the control exactly.
The constraint that made the rule awkward was our own.

** Not expressible as ruleset rules

Three controls need their own reads, and the audit does them:

- *deployment lock* — the issue or discussion body, plus =concurrency= groups
  in the workflow files. The audit checks =cancel-in-progress: false= on
  production: a cancelled production deploy leaves an indeterminate estate,
  which is worse than a queue.
- *freeze and emergency* — the change schedule, and the =EMERGENCY_CHANGE=
  repository variable as the fallback when the calendar is unreachable.
- *environment protection* — =environments/production= must carry a required
  reviewer for =change:normal=, =wait_timer= 0, and a =deployment_branch_policy=
  restricting which branches may deploy.

** Availability on the development box [E, 2026-09-13]

Against =aygp-dr/standard-change= the audit returns *eight unreadable
controls*: both =repos/{r}/rulesets= and the classic
=branches/main/protection= answer =403 Upgrade to GitHub Pro or make this
repository public=. Verified across 50 repos on this account: 35 public → 200,
15 private → 403, no exceptions. The org route is closed too — =defrecord= and
=walsh= are free orgs whose private repos 403 identically. Environment
/protection rules/ carry the same gate, so =environment: production= is
decorative here.

*This is a property of the development box, not of the model.* The target is an
enterprise account, where every control in the table above is available. The
finding is kept because it exercised something worth keeping — the audit's
four-outcome design. An audit reporting "no findings" because it could not read
anything would have laundered an unknown as a pass. That distinction is the
point; the plan tier is incidental.

** On an enterprise account

What becomes available, and what it changes:

| Capability                        | Effect on the model                                            |
|-----------------------------------+----------------------------------------------------------------|
| repo + *org-level* rulesets       | the guard table can be enforced once, across every repo, rather than configured per repo |
| environment protection rules      | guard 4's approval becomes platform-attested instead of a =pulls/{n}/reviews= query in a job |
| required reviewers as *teams*     | "a named change authority" stops being one person; §The balance's =prevent_self_review= deadlock dissolves |
| CODEOWNERS enforcement            | =change:normal='s path-based authority becomes native rather than a labeller convention |
| *custom deployment protection rules* | see below — this is the significant one |

*Custom deployment protection rules are the native home for guard 4b.* An app
can approve or reject a pending deployment /at deployment time/, which is
precisely the "at reliance, not at request" semantics the guard requires
(§Domain model, capabilities C4 and C5). On the free tier guard 4b has to be
synthesised from a =push= trigger on trunk plus label withdrawal
(=main-moved.yml=); on enterprise it can be a gate the platform itself asks
before letting the deployment proceed — attested rather than self-reported,
and revocable by construction.

That is worth designing toward now, because it changes /where/ the guard lives
without changing /what it says/. The predicate in §Domain model is unchanged;
only the encoding moves. Which is the claim §Domain model exists to make.

* What completes a deployment

The model had no answer to this, which is worth stating plainly because three
layers each invented a different one:

| layer         | what ended a deployment                                  |
|---------------+----------------------------------------------------------|
| =sim/=        | *a timer* — =clock >= activated + hold_slots=             |
| =tla/=        | *nothing* — =CompleteMerge= is enabled whenever guards pass |
| =gates/health.sh= | *one sample*, taken in two separate requests           |

None of those is a completion condition. A timer says time passed; an enabled
transition says nothing was forbidden; one sample says one replica answered.

** Three events, routinely conflated

| event     | means                                        | attested by                  |
|-----------+----------------------------------------------+------------------------------|
| *live*    | the estate serves the new build              | guard 5 (convergence)        |
| *closed*  | the change record is complete (ITIL)         | the PIR                      |
| *merged*  | trunk contains the change                    | the forge                    |

These occur in that order and are not interchangeable. *The berth protects the
estate but is released on =merged=, a repository event* — a mismatch worth
naming even though releasing slightly late is the safe direction.

** Convergence, not liveness [E, experiments/004]

Guard 5 now asserts that N samples all report the expected build, and reports
=UNCONVERGED= when it sees more than one. Measured against a deliberately
half-migrated fleet (two =apps/core= instances on different =BUILD_SHA= behind
an nginx round-robin):

- the old single-sample check *passed 6 of 12 runs* on an estate that was half
  unmigrated;
- the new check refused it every time (=4/8 samples not serving newsha22=);
- and passed once the old replica was retired (=8/8=).

The old failure mode is the dangerous kind: it *looks like flake*, so the
reflex is to re-run until green — and re-running until green is precisely what
manufactures a false pass against a mixed population.

** What it still does not know [P]

N samples is evidence, not proof. It cannot see a replica taking no traffic
during the window, it samples through whatever the load balancer chooses, and
it says nothing about in-flight requests still being served by the old build.
*A sampling check cannot establish convergence; it can only fail to refute
it.* Proof needs the platform to enumerate its own replicas and their
versions — which is capability C6, a platform-attested deployment record, and
is exactly why an attested record outranks a self-reported one.

So the honest answer: *a deployment completes when the platform says every
serving instance runs the new build, and in the absence of such a platform we
substitute repeated sampling and record that we have done so.*

* C6, and the one platform that can meet it

=docs/release-standards.org= found capability C6 — a *platform-attested*
deployment record — unmet on GitHub and unstandardised generally. Cloudflare
Workers *versions* appear to meet it [E from docs, [H] until run]:

| requirement                              | Workers versions                                   |
|------------------------------------------+----------------------------------------------------|
| promote the artifact staging tested      | upload and deploy are decoupled; deploy a version *by ID* |
| immutable artifact identity              | each version has a unique ID with creation metadata |
| a record written by the platform         | =wrangler deployments list= — Cloudflare's own      |
| rollback to a known previous artifact    | revert to a previously deployed version             |
| the served build is identifiable         | a Worker can set =x-build-sha= directly             |

Contrast =wrangler environments=, which are the *wrong primitive for a path to
production*: =[env.staging]= and =[env.production]= create *separate Worker
scripts* named =<worker>-<env>=, each deployed from source with =--env=. There
is no promotion between them, so production would run a script staging never
tested — the precise failure §Cloud mirror forbids. Environments separate
config; versions carry artifacts.

** Why GitHub cannot meet C6 and Cloudflare can

The difference is not API quality, and stating it properly generalises:

#+begin_quote
*A platform can attest to what is running only if it runs it.*
#+end_quote

GitHub's Deployments API records a claim about *someone else's* estate —
GitHub never contacts the target, so its record is a self-report with good
provenance about who claimed it. Cloudflare runs the Workers, so its record of
which version serves is a statement about its own estate. That is what makes
one attestation and the other bookkeeping, and it is the test to apply to any
future target: *does the recorder run the code?*

** Gradual deployment makes guard 5 ambiguous [P]

A Workers deployment may reference *two versions at once*, splitting traffic.
So a mixed fleet becomes an *intended state*, and guard 5 as written
(§What completes a deployment) reports =UNCONVERGED= for it — correctly by its
own definition and uselessly in context.

Guard 5 therefore needs an expected shape rather than a single expected build:
converged on X, or split between X and Y at a declared ratio. *Sampling cannot
supply that* — it cannot distinguish an intended 10% canary from an accidental
90%-stale estate without being told which to expect. Which is the argument for
C6 again from the other side: the platform's record says what the split is
*meant* to be, and sampling can then check the estate against it rather than
guessing.

Not implemented. Recorded because it falsifies the current guard 5 for any
canary or gradual rollout, which is the normal case for services.

* Every automation has a bypass

Not a concession — a design rule. An automation without a declared escape hatch
does not get obeyed, it gets *circumvented*, and a circumvention leaves no
record. §The balance already measured one instance of this: queue friction does
not slow delivery, it routes traffic onto =change:emergency=, the one path with
fewer checks. The general form:

#+begin_quote
*Design the bypass, or one gets invented in the dark.*
#+end_quote

The catalogue, so no future exception has to be argued from first principles:

| automation                    | bypass                | what it costs                        | where it shows |
|-------------------------------+-----------------------+--------------------------------------+----------------|
| labeller owns =app:*=         | =labeler:skip=        | manifest becomes asserted, not derived | label on the PR |
| staging must pass             | =change:emergency=    | no staging evidence for this change  | label + approval + PIR |
| the berth queue               | =change:emergency=    | jumps the queue                      | label + PIR    |
| the freeze window             | =EMERGENCY_CHANGE=    | the calendar stops governing         | repo variable  |
| guard 0 up-to-date            | =change:emergency=    | may ship behind trunk                | PIR records the divergence |
| *guard 2 gates green*         | *none*                | —                                    | —              |
| *guard 5 production healthy*  | *none*                | —                                    | —              |

** The two that have none, and why

The rule is not "everything has a bypass". It is:

#+begin_quote
Everything has a bypass *except the things whose bypass would make the system
lie about itself.*
#+end_quote

Guard 2 is the gate suite's verdict on this exact tree. Bypass it and every
downstream record — the staging pass, the PIR, the deployment record — asserts
a verification that did not happen. Guard 5 is the only evidence that what was
deployed is what is serving; bypass it and "deployed" becomes a claim rather
than an observation (§What completes a deployment).

Those are not stricter rules than the others. They are the rules that keep the
*other* bypasses honest: =change:emergency= is a legible, auditable act
precisely because the records it appears in are otherwise true.

** What makes a bypass acceptable

Three properties, and all three are required:

1. *Visible on the artifact* — a label on the PR, not a flag in a job.
2. *Carried into the record* — the PIR names it and who authorized it.
3. *Narrow* — it suspends one automation, not the guards around it.
   =labeler:skip= stops the labeller and touches no gate; =change:emergency=
   skips the staging environment and still faces guards 2 and 5.

A "force" flag fails all three, which is why =docs/idp-api.org= refuses to
offer one: a flag that appears to bypass guards 2 or 5 would be a lie in the
API surface.

* Labels as state transitions

The labels are not annotations on a process running elsewhere. *They are the
state machine*: an observation emits a label, and the label drives the next
transition. =staging:passed= is emitted by the authorizing e2e;
=production:healthy= by guard 5's convergence check; the merge is triggered by
=production:healthy=. Each transition is caused by *evidence*, not by a command.

That is a genuinely good property — a command-driven pipeline advances because
someone asked, and this one advances because something was observed — and it
has one sharp consequence.

** Three kinds of label, and only one is safe to trust

| kind          | examples                                      | emitted by      | forgeable? |
|---------------+-----------------------------------------------+-----------------+------------|
| *derived*     | =app:*=, =change:standard=, =change:normal=   | the labeller, from the diff | no — resynced on every push |
| *request*     | =change:requested=, =change:emergency=, =labeler:skip= | a human   | yes, and that is the point  |
| *action*      | =deploy:staging=, =deploy:production=         | the workflow    | no — it marks work in flight |
| *observation* | =staging:passed=, =production:healthy=, =change:scheduled=, =blocked:*= | a gate or the scheduler | *yes, and that is a hole* |

A request label is meant to be added by hand. An observation label is a
*claim that something was measured*, and GitHub does not distinguish a label
written by a workflow from one typed by a person with write access.

** The name shape tells you the kind

Worth keeping deliberately, because it makes the taxonomy readable without a
table:

| shape                    | kind        | reads as              |
|--------------------------+-------------+-----------------------|
| =change:requested=       | request     | someone asked         |
| =deploy:<env>=           | action      | work is happening     |
| =<subject>:<state>=      | observation | *a check came back*   |

=production:healthy= and =staging:passed= are subject-plus-observed-state, and
they read as check results because that is what they are. =deploy:staging= is a
verb plus a target and reads as an action because it is one. A new label whose
name does not fit one of these three shapes is probably conflating two kinds —
which is how =deploy:staging= spent three revisions in the request column.

** The check list is the pipeline, visible

Because every stage is a workflow keyed on a label, a PR shows the whole shape
at once — including the parts not taken:

#+begin_example
1 in progress, 3 skipped, 1 successful
  in progress   gate / gate-selftest
  skipped       deploy-staging / deploy
  skipped       promote / promote
  skipped       deploy-production / deploy
  successful    labeller / label
#+end_example

That is the state machine rendered by the forge for free, and it is worth
having on purpose rather than by accident. One caveat on reading it: GitHub
says *skipped* for "the =if:= condition was not met", which looks like a
decision and is really just "not this change's turn yet". A stage that is
skipped because =change:requested= has not been added is indistinguishable, in
that list, from one skipped because a guard refused.

** So a label-triggered workflow must re-derive, not trust

#+begin_quote
If a label drives behaviour, the workflow it drives must re-establish the fact
the label asserts. Otherwise the label is a self-report, and adding it by hand
is a privilege escalation.
#+end_quote

Concretely: hand-adding =production:healthy= would trigger
=merge-on-healthy.yml= with *no deployment having happened at all*. The fix is
not to police who may add labels — it is for the merge job to run guard 5
itself rather than believe the label. Which is the same rule as §C6 (*a
platform can attest to what is running only if it runs it*) and the same rule
as guard 4b (*re-check at the moment of reliance*), arriving a third time in a
new place.

This is why =gates/health.sh= is cheap and idempotent by design: it is meant to
be run again at every point that depends on its answer, not once with the
result cached in a label.

* Speculation — production:healthy as a merge trigger

The mechanism is trivial and the ordering is not.

** The trigger

#+begin_src yaml
# reference implementation of .github/workflows/merge-on-healthy.yml -- that file is the source
name: merge-on-healthy
on:
  pull_request:
    types: [labeled]
permissions: {contents: write, pull-requests: write}
jobs:
  merge:
    if: github.event.label.name == 'production:healthy'
    runs-on: ubuntu-latest
    env: {GH_TOKEN: "${{ github.token }}", PR: "${{ github.event.pull_request.number }}"}
    steps:
      - uses: actions/checkout@v4
      # Guard 4b at the merge, AGAIN. production:healthy says the estate is
      # serving this change; it says nothing about whether main has moved
      # since. If it has, merging now would land a tree that predates what is
      # already on main -- and production is ALREADY serving it.
      - name: main must not have outrun production
        run: |
          state=$(gh pr view "$PR" --json mergeStateStatus -q .mergeStateStatus)
          case "$state" in
            BEHIND|DIRTY)
              gh pr comment "$PR" --body "PRODUCTION IS AHEAD OF MAIN. \`production:healthy\` is set and the estate is serving this change, but \`main\` moved during the deploy (\`$state\`) so the merge is refused. This is not recoverable by merging -- see spec.org, the divergence below."
              gh pr edit "$PR" --add-label blocked:diverged
              exit 9 ;;
          esac
      # Re-derive the fact the label asserts. GitHub cannot distinguish a label
      # written by a gate from one typed by a person, so trusting
      # production:healthy would make hand-adding it a way to merge with no
      # deployment at all.
      - name: guard 5, again — do not trust the label that triggered this
        run: |
          SHA=$(gh pr view "$PR" --json headRefOid -q .headRefOid | cut -c1-7)
          HEALTH_MODE=manifest HEALTH_SAMPLES=5 \
            ./gates/health.sh "${{ vars.PRODUCTION_URL }}" "$SHA" \
            || { gh pr edit "$PR" --remove-label production:healthy
                 gh pr comment "$PR" --body "production:healthy was set but production is not serving \`$SHA\`. The label has been withdrawn and the merge refused."
                 exit 1; }
      - run: ./change/release.sh "$PR"
#+end_src

** The divergence it exposes

Production deploys *before* the merge (§What completes a deployment names the
three events: live, closed, merged, in that order). So between =production:healthy=
and the merge there is a window where *the estate is serving a change that
trunk does not contain*. If the merge is then refused, the system has no
defined state for it:

| divergence              | recoverable by | cost                          |
|-------------------------+----------------+-------------------------------|
| main ahead of production | deploying      | a deploy — routine            |
| *production ahead of main* | *rolling back* | *undoing a change users have already seen* |

The second is strictly worse, and the current ordering produces it. Which
suggests the inversion:

** The inversion worth considering [H]

*Merge first, deploy from main.* Then the only reachable divergence is
main-ahead-of-production, which a deploy fixes. Production can never be
serving something trunk does not contain.

What it costs is exactly what =docs/release-strategies.org= found: "on main"
becomes *intent* rather than *evidence*, because the merge now happens before
the production evidence exists. You trade a divergence that needs a rollback
for one that needs a deploy, and you pay for it by weakening what a merge
means.

*Neither ordering is free, and this model currently picks the expensive one
without having said so.* Blue/green makes the rollback cheap enough that the
current ordering is defensible — =switch.sh= back to the previous colour is a
second, and the previous build is still live on the idle jail. Without
blue/green it would not be.

** What is still unencoded

Even with the guard above, =blocked:diverged= names a state the model cannot
*resolve*: production is serving X, main does not contain X, and no guard says
which should win. Resolving it needs the A5 question — *is this change
reversible?* — which none of the six guards asks (§Changelog, D-05). The
trigger is implementable today; the state it can land in is not.

* The balance

*The objective is protecting production. The queue and the calendar are
instruments.* Stating the hierarchy matters because every control below has a
cost, and a control whose cost is paid in queue time buys nothing unless it
also reduces the chance of an unsafe change reaching the estate.

** What each control actually protects

| Control              | Protects                | Cost is paid in       | Removable? |
|----------------------+-------------------------+-----------------------+------------|
| gate-selftest        | *production*            | seconds               | no         |
| lint / test / e2e    | *production*            | minutes               | no         |
| guard 5 health check | *production*            | seconds               | no         |
| guard 4 staging pass | *production*            | one staging cycle     | emergency only |
| guard 4b divergence  | *production*            | a rebase, when earned | no         |
| guard 0 up-to-date   | production, indirectly  | a rebase              | emergency only |
| guard 1 singleton    | *the queue*             | berth wait            | *negotiable* |
| freeze window        | *the calendar*          | berth wait            | *negotiable* |
| change window record | nothing; it is a record | nothing               | n/a        |

The first five are load-bearing: each closes a path by which a broken or
unauthorized change reaches production. The last three are coordination
devices. They are worth having, and they are the ones to tune.

** Over-protecting the queue costs production safety

This is not the usual safety-versus-speed trade, and reading it that way gets
the sign wrong.

=change:emergency= is the *one* label that bypasses staging. So queue friction
does not convert into slower-but-equally-safe delivery — it converts into
traffic on the path with fewer checks. =gates/simulate-gates.py= models it: at
a mean berth wait of two hours roughly a sixth of changes route to emergency;
past about four hours it is the majority. Tighten the queue far enough and you
have optimised your way to *less* production safety, through the only door you
deliberately left open.

The practical consequences, which is why this section exists rather than being
a remark:

- *Guard 1's singleton is the tunable*, not the gates. If the berth is the
  bottleneck, add berths (per-team paths to production, §What this model gets
  wrong) before weakening a gate. Never trade a gate for throughput; the gates
  are the reason the berth is worth holding.
- *Freezes should be narrow and few.* A freeze protects the calendar's promise,
  not the estate. A standing freeze is indistinguishable from a slow queue, and
  produces the same emergency traffic.
- *A change window is a record and must never block.* The moment writing it can
  fail closed, a calendar outage becomes a deployment outage — see
  §Change enablement, preflight exit 4, and the =EMERGENCY_CHANGE= fallback that
  exists precisely because a /record/ was allowed to become a /control/.
- *Watch the emergency rate as the health metric.* A rising share of
  =change:emergency= is not an incident trend; it is the queue reporting that it
  has become too expensive to use honestly. The PIR already records every
  bypass, so the number is free.

** Where the balance was already wrong, and corrected

Guard 4b as first written withdrew =deploy:staging= on any movement of =main=.
That protected production and charged the queue for it indiscriminately — a
README merge cost a full rebase and re-verify, and the berth went back to the
pool. Both corrections in §Guard 4b are this section applied:

- *classify the divergence*, so only =artifact= and =hotfix= — the classes that
  can actually change what ships — withdraw the staging pass;
- *never take the berth for someone else's merge*, because losing your position
  to another team's emergency is a cost with no corresponding safety gain.

The test for any future control is the same: name what it protects, name who
pays, and if the answer is "the queue pays and production gains nothing",
it is ceremony.

* At scale

The design has a throughput ceiling and it is the berth. =gates/simulate-gates.py --soak=
computes it: 20 checks at 95% with one re-run of failed checks, 30-minute
holds.

| PRs | berths | wall-clock | mean wait | emergency share |
|-----+--------+------------+-----------+-----------------|
| 100 |      1 |     2.2 d  |    26.3 h |          100.0% |
| 100 |     16 |     0.1 d  |     1.6 h |           13.2% |
| 200 |      1 |     4.4 d  |    52.6 h |          100.0% |
| 500 |      1 |    11.0 d  |   131.4 h |          100.0% |
| 500 |     16 |     0.7 d  |     8.2 h |           98.6% |

*A few hundred changes through one berth is weeks of serialized wall-clock*,
and §The balance says what that queue wait does: the emergency share goes to
100%. So a high-volume soak run against a singleton berth *does not test the
pipeline — it tests the bypass*, because by the time the queue is that deep
every change has a rational reason to carry =change:emergency=.

Read that carefully, because it is not a defect the soak /finds/. It is the
singleton berth meeting its design limit, exactly where §What this model gets
wrong, on purpose says it would. The remedy is the one already named there —
hard contracts at the router and a path to production per team — and the scale
requirement is *evidence for that fix rather than a new problem*.

Consequences for anyone planning a soak:

- *Do not measure gate behaviour through a deep queue.* Guards 2 and 5 are
  per-change and can be soaked at any N. Guards 0, 1 and 4b are queue
  properties and are the thing under test; conflating them wastes the run.
- *Widen the berth first, or shorten the hold.* Sixteen berths brings 100 PRs
  to under a day. At 500 even sixteen is not enough, which is the honest signal
  that per-team paths to production — not more berths on one path — is the
  answer.
- *Or do not run it at all.* =gates/pbt-pipeline.py --exhaustive= already
  enumerates every ordering at a small bound, deterministically, in seconds. A
  soak buys evidence about /volume and flake/, not about /correctness of the
  guards/; the exhaustive model already settles the latter more completely than
  any number of PRs could.

* The defect taxonomy

Written for a rebuild, and deliberately general. None of it is specific to this
monorepo, these four apps, or GitHub — every entry was found here, but each one
is a property of *deployment pipelines that judge things*, and any pipeline that
does not defend against them will reproduce them.

Seventeen scenarios in =scenarios.org= are the evidence. This section is the
pattern behind them.

** The eight classes

*** 1. Unreachable is not falsified

/I could not check/ silently becomes /no/. Fired at least six times here.

A check run that never started reports =failure= identically to one that ran and
failed. A health probe that could not connect counts as unhealthy. A scheduler
call that errored is read as "no window". A reviews endpoint that is down is
read as "the head moved".

*The rule:* every read has *three* answers — yes, no, and *I could not
determine*. The third must refuse differently from the second, and an
implementation that cannot express it is unusable.

*** 2. Superseded is not current

The same mistake on a different axis. A verdict that was true is used after it
has been replaced.

Every check run stays attached to its commit forever, so the failures from
before an outage sit beside the successes after it. A guard that counts history
refuses on evidence that no longer applies.

*The rule:* take the *latest* observation per name, at the moment of reliance.
A check run has three independent facts the API flattens into one field: *did it
run*, *when*, and *what did it conclude*. Read =conclusion= alone and you get
both this class and the one above, silently.

*** 3. Observation is not intent

A claim written from what somebody meant rather than what was measured.

A marker script that takes =--samples 99999 --observed-by my-own-fingers= and
renders them as an observation. A deployment record posted before the health
check. A label asserting a measurement nobody took.

*The rule:* an instrument records its own result. Where a value genuinely comes
from the caller, the record must *say so by name*. "Someone said it was fine" is
not evidence; "=gates/smoke.sh= observed pass on =a013270=" is.

*** 4. A verdict must name the build it is about

"Staging passed" is not a fact. "Staging passed at =a013270=" is.

Without it a change is authorized by evidence about a different artifact — the
defect is invisible because every label is present and green.

*The rule:* every observation, approval and deployment record carries the commit
it was measured against, and every guard compares that to the head *at the
moment it relies on it*.

*** 5. A guard checked once is not a guard

The subject moves between the check and the reliance.

A window verified at booking and never re-read. A class set two hours ago and
trusted now. A berth claimed before a freeze was declared.

*The rule:* a guard whose subject can change must be re-derived at the moment it
is relied on. Correct-by-construction beats notified-on-change, which is why the
absence of an event bus is survivable here at all.

*** 6. Formatting destroys verdicts

=cmd | sed= makes =$?= sed's. =cmd | tail= makes it tail's.

This shipped an UNHEALTHY estate as =passed=, and — eight hours after being
fixed in one script — was rebuilt from scratch in the driver written to replace
it. *Knowing the rule did not prevent writing the code*, because piping a
command to trim its output is the obvious thing to type.

*The rule:* any step whose verdict gates a later step must be *unpipeable*.
Capture, test, then format. Formatting happens after the verdict is in hand,
never in the expression that produces it.

*** 7. A check that cannot fail produces no verdict

The =gate-selftest= rule, generalised.

Three tests here passed while asserting nothing: a =typeof === 'string'= check
satisfied by a request-derived value, an escaping assertion satisfied by an
unrelated =&amp;= elsewhere on the page, and an =esc()= call whose deletion failed
zero tests. All three were found by *mutation*, never by reading.

It applies to a single change as much as to a suite: a diff no assertion covers
is a diff the pipeline cannot judge, and every gate will be green.

*The rule:* mutate the thing the check is about and require the check to fail.
A suite that has never rejected anything is one nobody has tested.

**** The guard-level form: count lifetime refusals

Measured 2026-09-14, and it is the reason this class is listed first among the
meta-observations. TWO of this pipeline's guards have never refused anything:

| guard | lifetime refusals | why |
|-------+-------------------+-----|
| 0 (up to date with main) | *0* | its body is inside =if state = "BEHIND"=, and the forge has never returned =BEHIND= — 0 of 14 open PRs, after 163 commits landed on main in one night |
| 6 (production-first) | *0* | 484 workflow runs, never once triggered by a label |

Both guards are correctly written. Reviewing the body of either finds nothing,
because there is nothing wrong with it — what is wrong is that *the trigger
condition is supplied by an oracle that does not emit that value in this
configuration*. See =scenarios.org= D17.

So the audit question for a guard is not "is the logic right". It is: *has this
ever refused anything, and if not, can the oracle it reads ever emit the value
it tests for?* A guard with zero lifetime refusals is a hypothesis, not a
control.

*** 8. Not yet is not no, and cannot say is not either

The third value. Every question a pipeline asks an external system has at least
three answers — *yes*, *no*, and *I cannot tell you yet* — and every defect in
this class is the third being folded into one of the first two.

Class 1 (/unreachable is not falsified/) is this one's sibling and is about a
system that did not answer. This is about a system that DID answer, in its own
documented vocabulary, to say it has no answer. That is harder, because the
response looks like data: it parses, it is not an error, and a guard that reads
it gets a value rather than an exception.

**** What the forge actually returns, and what it does not mean

Every row here was hit while driving changes on 2026-09-13/14. None is a bug in
GitHub; each is a documented state we had no contract for.

| asked | the answer | what it does NOT mean | what we did |
|-------+------------+-----------------------+-------------|
| =mergeStateStatus= | =UNKNOWN= | up to date with main | guard 0 printed =ok mergeStateStatus=UNKNOWN= and proceeded |
| =mergeStateStatus= | =UNSTABLE= | not mergeable | mergeable, with a non-required check failing — correctly proceeded |
| a check run | =status=in_progress=, =conclusion=null= | failed | counted as "not green"; it had not finished |
| a check run | =conclusion=skipped= | failed | counted as "not green"; nothing ran, so nothing failed |
| a check run | an older run for the same name | a current verdict | counted alongside the run that replaced it (class 2) |
| a just-written label | absent on the next read | never written | guard 4 refused #57 one minute before reading it as authorized |
| =schedule.sh current= | exit 1 | no window | also returned for a calendar it could not read |
| a health probe | HTTP =000= | unhealthy | unreachable; the estate was never observed |

**** Why =UNKNOWN= in particular is the dangerous one

GitHub computes mergeability asynchronously. Ask immediately after a push, or
after main moves, and =mergeStateStatus= is =UNKNOWN= — not because the branch
is bad but because nobody has looked yet. It resolves in seconds.

A guard that treats =UNKNOWN= as *pass* deploys a branch nobody has checked is
up to date. A guard that treats it as *fail* refuses every change for the first
few seconds after trunk moves, which on a busy queue is every change. Both are
wrong, and the second is the one that looks careful.

*The rule:* the third value is never a verdict. Poll until the answer is one of
the two real ones, with a bound; if the bound expires, that is class 1 —
=indeterminate=, said out loud, not resolved in either direction. A guard whose
return type is a boolean cannot express this, which is why every verdict enum in
this spec is three-valued.

**** The read-after-write case is the same defect in time

An observation is recorded, and the guard that requires it reads before the
write is visible. /Not there/ and /not there YET/ are the same bytes. It is
class 8 rather than a race condition because the fix is not a lock — it is that
the reader must be able to say "I cannot tell yet" instead of "no".

Worse, recording the observation can invalidate the authorization it was taken
for: writing =staging:uat= retriggers the CI suite, so the four gate check runs
go to =in_progress= and guard 4 — which requires them green — refuses. Driving
#63 hit exactly this. The measurement and the thing measured are coupled through
the forge, and nothing in the pipeline declared that edge.

** Two meta-observations

*** The taxonomy is recursive

Each rule has been violated *by the code written to enforce it*. The approver
manufactured its own verdict. The label auditor flagged the comment explaining
the fix. The check-run guard read stale runs. The driver swallowed the refusal
it existed to honour.

A rebuild should expect this rather than be surprised by it, and should test its
guards the way it tests its gates.

*** The failures are silent, and that is the point

Every class above *refuses things that should pass* or *passes things that
should refuse*, and neither breaks loudly. Nothing crashes. The pipeline reports
success. The only way any of them surfaced was a human watching a board, or
running a mutation, or reading an error message instead of a summary.

* Boundary conditions

Stated as the states a pipeline can reach, not as things to avoid. A rebuild is
graded on whether it can *name* each one, not on whether it prevents them.

| state | meaning | correct handling |
|-------+---------+------------------|
| *deployed, not merged* | production serves a build trunk does not contain | name it; the next change branches from a trunk without it and reverts it |
| *merged, not deployed* | trunk has it; the estate does not | ordinary, and fine — the next deployment carries it |
| *authorized, unbooked* | every observation taken, no reservation | eligible; needs one command |
| *booked, unverifiable* | a reservation on a change no gate can judge | refuse at booking, not at activation |
| *expired* | a reservation lapsed unattempted | *not a failure.* Its own closure code; the change keeps everything else |
| *refused* | a guard said no | a successful outcome. Never worked around |
| *backed out* | production served it and was returned | asserts an estate action; must name what it returned to |
| *undefined class* | zero or two classifications | every rule branching on the class takes an arm nobody chose |
| *stale evidence* | observations about a build that is no longer the head | refuse; do not re-stamp |

** The one that has no name yet

*Deployed, not merged, and the window is gone.* Three facts at once, and nothing
in the vocabulary says it. Reached twice here — once by a script that reported
the opposite. A rebuild needs a word for it before it needs a fix.

* The label grammar — intent drives, everything else annotates

Added 2026-09-14, after five independent models of this pipeline disagreed
about what several labels MEAN. They did not disagree about the code; they
disagreed about the vocabulary, which is a sharper problem.

** The rule

*A label either DRIVES or it ANNOTATES, and only intent drives.*

| shape | role | example | what it is |
|-------+------+---------+------------|
| =app:<group>= | *manifest* | =app:pdp= | WHAT moves. Derived from the diff; the blast radius |
| =itil:<class>= | *classification* | =itil:standard= | WHICH RULES apply |
| =release=, =release:start= | *intent, change scope* | | ship this change |
| =deploy:<env>= | *intent, environment scope* | =deploy:staging= | START the deploy process into that environment |
| =<env>:<action>= | *annotation* | =staging:e2e= | what HAPPENED inside that environment |
| =blocked:<cause>= | *annotation* | =blocked:queue= | why a refusal happened |
| =change:<state>= | *annotation* | =change:complete= | how far it got |
| =freeze=, =emergency= | *estate* | | properties of the WORLD. No prefix, because there is no subject to name — they are not about a change |

Read the colon twice. In =app:=, =itil:=, =change:=, =deploy:= and =blocked:=
the part BEFORE the colon names the subject and the part after is its value. In
=staging:e2e= and =production:healthy= the part before the colon is the
ENVIRONMENT and the part after is the action taken in it. Two grammars, one
punctuation mark, which is why the models disagreed.

** Why =deploy:<env>= is intent and not state

This is the correction. =change/label-owners.tsv= describes =deploy:staging= as
"ACTION in flight, and the berth claim guard 1 reads" — one label doing two
jobs. Saying /start deploying to staging/ and /this change holds the only berth/
are different facts with different lifetimes, and conflating them is how the
stranded berth happens: the intent is satisfied or abandoned while the lock
stays, and nothing owns releasing it. #92 sat holding =deploy:staging= with no
window and its build serving nowhere; the intent was long dead and the lock was
not.

Under this grammar the intent is the label and *the berth is a consequence of
the intent, not the same object as it*. A rebuild should carry the lease
separately, with an owner and a deadline, so that "who is deploying" and "who
holds the path" can disagree out loud.

** What the actions inside an environment are for

=staging:e2e=, =staging:smoke=, =staging:uat=, =production:healthy= are
ANNOTATIONS: they record that an instrument measured something. They never
start anything. Their authority does not live in the label at all — it lives in
the observation record the instrument wrote, which names the build. The label
is a convenience that goes stale; the record is the evidence that does not.

That is why guard 4 reads records rather than labels, and why staging can be
gone entirely and 74 of 100 changes still ship (scenarios.org D18).

** Measured: where the vocabulary's weight actually sits

From =experiments/021-state-machines/merge.py=, over five independent models —
share of all label-driven transition mass:

| role | share | what it is |
|------+-------+------------|
| *the core three* (=app:=, =itil:=, =release=) | *22.8%* | everything a person CHOOSES |
| estate (=freeze=, =emergency=) | *36.6%* | two labels, about the world |
| gatekeeping (=blocked:=, =deploy:=, =change:requested/scheduled=) | 30.3% | who may move, and when |
| audit (=<env>:<action>=, =change:complete/failed/backed-out=) | 9.0% | the trail a PIR is written from |
| override (=labeler:skip=, =hold:staging=, =itil:emergency=, =review:proxy=) | 5.6% | getting past a refusal, on the record |

*Two thirds of this machine's movement is gatekeeping and estate.* A person
chooses three things; the pipeline spends most of its state changes on blocking
and on world-state. Two estate labels move it more than every deliberate choice
combined.

That is not obviously wrong — a control system should spend its time
controlling — but it is worth a rebuild knowing before it starts, because it
says where the complexity will actually be. It will not be in the part anyone
demonstrates.

* Nomenclature

The terms a rebuild must keep, and what each one is *not*.

| term | is | is NOT |
|------+----+--------|
| *change* | a proposed alteration, with a record | a commit, a branch, or a PR |
| *classification* | what KIND of change (standard/normal/emergency) | how far it has got |
| *lifecycle* | how far it has got (requested/scheduled/complete/failed/backed-out) | what kind it is |
| *window* | an entitlement to the path to production | permission to deploy, or a promise it will happen |
| *berth* | the singleton lock on the path | a lock on an environment |
| *observation* | a measurement, by an instrument, naming a build | a label |
| *record* | what survives the labels being cleared | the working state |
| *freeze* | a property of the WORLD, blocking everyone including its declarer | a property of a change |
| *emergency (estate)* | something is wrong out there; everything stops | a classification |
| *itil:emergency* | THIS change is the remedy; it passes the freeze | the estate being shut |
| *closure code* | passed / failed / cancelled / expired / backed-out | a boolean |
| *soak* | proof by use across N *later* deployments | a longer test run |
| *control-plane* | changes how every FUTURE change is verified | a big change |

** The distinction that carries the most weight

*Classification and lifecycle are different namespaces about different subjects,
and the estate is a third.* Collapsing any two of them produces the defect that
took the longest to find here: one label asserting both "an emergency is in
flight" and "this change is exempt", so the write that closed the estate
exempted its own carrier.

Three subjects, three namespaces, and the estate's belongs on something that
cannot be deployed.

* Version

Bumped to *0.5* on 2026-09-14. The three sections above — the defect taxonomy,
the boundary conditions and the nomenclature — are new, and they are what a
rebuild is graded against.

0.4 described a pipeline. 0.5 describes *what goes wrong in pipelines that judge
things*, which is the part that generalises. A reader outside this repo can use
the taxonomy and the nomenclature without adopting a single line of the
implementation.

*The deviation, recorded rather than tidied:* the version sat at 0.4 through
roughly 180 commits on 2026-09-13, including sixteen scenarios, four ADR
sections and the whole grind. It should have moved earlier.

* Refutation conditions

Concretely, what observation falsifies this repo's central claim — /that the
gate sequence prevents an unauthorized or unsafe production deployment/:

1. A production deployment completes while =blocked:lock= or =blocked:freeze=
   is set, or while another production run holds the lock.
2. =gates/preflight.sh= exits 0 when the change schedule is unreachable and no
   =EMERGENCY_CHANGE= variable is set (it must exit 4 and block).
3. Any gate passes on a =gates/fixtures/<gate>/fail/= input, or fails on the
   matching =pass/= input. Either direction voids the gate.
4. =deploy-production.yml= deploys a group that has no
   =deployed:staging-<group>-<n>= label on the head SHA.
5. A =change:normal= PR reaches production with no approving review.
6. The rendered nginx config and =router/routes.json= disagree while
   =gates/lint.sh= exits 0.
7. A stale lock older than 60 minutes is cleared without the clearance
   appearing in a PIR.
8. The share of changes carrying =change:emergency= rises without a matching
   rise in incidents — the queue has become too expensive to use honestly, and
   the bypass is absorbing normal traffic (§The balance).

Each is a test to be written, not merely a worry. Until they exist as
executable checks, the central claim is [H].

* Host survey of record

PHASE 0, run 2026-09-12 on hydra. Nothing below is recalled; each line was
observed.

| Tool      | Version / path                                  |
|-----------+-------------------------------------------------|
| OS        | FreeBSD 14.4-RELEASE-p6 GENERIC amd64 (hydra)   |
| node      | v24.14.1 =/usr/local/bin/node=                  |
| npm       | 11.11.0                                         |
| deno      | 2.6.6 (x86_64-unknown-freebsd)                  |
| gmake     | GNU Make 4.4.1                                  |
| make      | BSD make (reports no =--version=)               |
| jq        | 1.8.1                                           |
| gh        | 2.83.2, authenticated as =aygp-dr=              |
| git       | 2.53.0                                          |
| sqlite3   | 3.50.4                                          |
| python3   | 3.11.15                                         |
| emacs     | GNU Emacs 30.2 (tangles this file; 6 blocks [E]) |
| chromium  | 148.0.7778.215 as =/usr/local/bin/chrome=       |
| nginx     | 1.30.4 (installed 2026-09-13 after =pkg update=) |
| eslint    | *not installed* (project-local dep)             |
| wrangler  | *not installed*                                 |
| podman    | 5.8.1 — but FreeBSD jails, not Linux OCI images, and rootless unsupported |
| playwright| 1.63.0 installs, but *cannot run on FreeBSD* — see Known blockers |

Repo state at survey: zero tracked files, no commits, no GitHub remote.

Second host, run 2026-09-14 on the mini (macOS). Each line observed.

| Tool      | Version / path                                   |
|-----------+--------------------------------------------------|
| OS        | Darwin 24.1.0 arm64 (mini)                       |
| node      | v26.2.0                                          |
| gmake     | GNU Make 4.4.1                                   |
| jq        | 1.7.1                                            |
| gh        | 2.100.0, authenticated as =jwalsh= and =aygp-dr= |
| python3   | 3.14.6 default — *no Hypothesis*; 3.11.10 at =/opt/homebrew/opt/python@3.11/bin= has 6.111.0 |
| emacs     | GNU Emacs 32.0.50 (tangle, docs gate, forge all [E]) |
| java      | OpenJDK 21.0.4; =tla/check.sh= all seven runs [E] |
| shellcheck| 0.10.0                                           |
| docker    | =/usr/local/bin/docker= present; not exercised    |
| podman    | =/opt/homebrew/bin/podman= present; not exercised |
| container | Apple =container= *not installed*                |
| nginx     | not exercised; router = =router/server.js=       |
| forge     | magit 20260719.948 + forge via MELPA; =gmake forge= lists 25 PRs [E] after the revoked =^forge= token was replaced |
Neither =aygp-dr/slipway= nor =aygp-dr/standard-change= exists on GitHub.

** What was not verified, and why

- No gate was run — none exists yet. Every matrix cell is therefore [H].
- Tangling /was/ run [E]: =emacs --batch -l org --eval '(org-babel-tangle-file
  "spec.org")'= emits all 6 blocks and creates nested directories, confirming
  the file-level =:mkdirp t=. It surfaced two defects, below.
- Playwright against system Chromium on FreeBSD was not attempted (no app to
  drive). This is the single largest open portability risk.
- The Google Calendar used as the change schedule was not contacted; the
  account, calendar id, and credential path are unstated (see Open items).
- (resolved) =pkg update= was run with root on 2026-09-13; nginx 1.30.4 and
  gitea-act_runner 0.4.0 installed.
- The GitHub and Cloudflare targets were not exercised.

* Open items (tracked as issues, not here)

- Slot count per group (initial: 2 per group; trigger to raise: two consecutive =blocked:lock= on staging for the same group).
- Whether =change:normal= also requires a CAB-style label from a second reviewer (trigger: first incident caused by a router change).
- Calendar as the freeze source vs repo variable only (trigger: first preflight exit 4 in a real run).
- *(closed)* Directory name vs repo name. =slipway= and =standard-change= are
  the same project under two names, and both checkouts point at the same
  remote. Not a deviation — the working-copy path is immaterial to everything
  the spec governs, which is the point §Domain model makes about encodings.
- *Guard 2 was unimplemented on the emergency path* — *fixed*. The check-run
  query lived only in =promote.yml=, which is gated on =staging:passed=, so an
  emergency reaching production never evaluated it. Found by auditing the
  workflows against the spec's own claims rather than by any test, because no
  test exercises real workflows yet. Trigger for re-check: the first live
  emergency run.
- *Ruleset enforcement is unavailable* on a private repo on the current plan
  (403 on both the rulesets and branch-protection APIs) [E]. Until resolved,
  every ruleset control is advisory and only the workflow-level guards are
  real. Trigger: before anyone claims the pipeline is enforced.
- *Change-schedule identity*: which Google account owns the calendar, its
  calendar id, and how =change/schedule.sh= authenticates in a runner. Blocks
  any real =preflight= exit-3 path. Trigger: first implementation of
  =change/schedule.sh=.
- *(refuted 2026-09-13)* Playwright on FreeBSD. =playwright-core= throws
  =Unsupported platform: freebsd= at module init. Hydra's e2e cell is
  unsupported and the Playwright gate is CI-only; =gates/e2e.sh= covers local.
- *=ports.tsv= tangled with spaces, not tabs* [E, observed] — *fixed*. The
  block now carries real tabs; regression check is =grep -cP '\t' ports.tsv=.
- *=ports.tsv= was both a =:tangle= target and mutable state* — *fixed*, and
  the trigger fired exactly as predicted: creating the second worktree, a
  re-tangle restored a stale row and displaced both worktrees by one block.
  The =:tangle= is gone; =ports.sh alloc= creates the file.
