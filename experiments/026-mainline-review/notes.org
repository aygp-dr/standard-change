#+TITLE: 026 -- the mainline review, run log
#+DATE: 2026-09-15

* The shape

One heading per run, dated, recording what was /observed/. The driver agent
fills the headings below in order and adds nothing outside this directory.
See =README.org= §The review process for what each step must record.

* 2026-09-15/16, the full mainline review (185 commits)

** Step 0: Pinned frame

- HEAD: 9560471
- git status: clean (only untracked files under experiments/026-mainline-review/)
- base: d4776a6
- date: Wed Sep 16 01:57:09 UTC 2026
- host: Darwin 24.1.0

** Step 1: Extract the ledger

#+begin_example
base ref            d4776a6
ledger              experiments/026-mainline-review/ledger.tsv
commits             185
merge commits       0

by cell
  hydra             103
  mini              82
by kind
  apps              39
  docs              32
  change-script     27
  experiment        20
  gate              15
  spec              13
  chore             13
  model             13
  research          13
by protocol
  not-required      130
  missing-note      50
  ok                5

notes present       10
notes required      55
notes missing       50
notes incomplete    0
has Timeline        10
has Reproduction    10

rows citing issues  94
rows with repro cmd 10
repro cmds that write 3

judgement cells to fill  1480
#+end_example

Count verification: =git rev-list --count d4776a6..main= produces 185. PASS.

** Step 2 & 3: Caches

Forge caches built once:
- cache-issues.json: 400 issues/prs cached from forge
- cache-prs.json: 400 issues/prs cached from forge

Both caches available. No =gh= calls failed.

Baseline checks, all run once:

#+begin_example
./gates/docs-lint.py
  exit code: 0
  output: 91 org files, 0 findings

python3 tla/frames.py --check
  exit code: 0
  output: (none, passed)

python3 gates/label-audit.py
  exit code: 1
  output: 47 labels declared, 24 written in-tree, 13 finding(s)

python3 gates/label-model-coverage.py
  exit code: 1
  output: 17 label(s) in the pipeline and in no model

python3 sim/label_sim.py --bound 8 --walks 0
  exit code: 1
  output: VERDICT violated NoUnbookedDeploy
#+end_example

** Step 13: Close the run

*** Verdict counts

#+begin_example
  confirmed-by-note-only    7
  no-claim                  7
  unverifiable            171
  ────────────────────────
  TOTAL                   185
#+end_example

*** Protocol value counts

#+begin_example
  not-required             130
  missing-note              50
  ok                         5
  ────────────────────────
  TOTAL                     185
#+end_example

*** Missing-note commits (protocol defects): 50 total

By kind:
#+begin_example
  change-script    23
  gate             14
  spec             13
#+end_example

By cell:
#+begin_example
  hydra            36
  mini             14
#+end_example

The 50 missing-note commits are flagged as protocol defects. These are commits
that the protocol declares must carry a note (Timeline + Reproduction) but do not.

Full list of 50 missing-note shas:

#+begin_example
7d02a66 hydra gate fix(guards): superseded is not current
76553c9 hydra spec spec: 0.5 -- defect taxonomy, boundary conditions
de9f25f hydra change-script fix(activate): read the current check run
d98796e hydra change-script fix(activate): deploy the apps, not the stand-in
c43bf36 hydra change-script fix(soak): zero probes is not a pass
16aa82a hydra spec spec: defect class 8 -- not yet is not no
3cc2740 hydra spec spec: exposure, the LAN boundary
faf7c6e hydra spec spec: guard 6 (production-first) gets a predicate
ca06945 hydra spec scenarios D17 + spec: two guards have never refused
f3fe16d hydra change-script feat(marker): deployment marker
45c1163 mini spec spec: macOS cell observed
7473431 hydra change-script fix(abort): clear deploy:production
29a9cc1 hydra change-script fix(activate): three live D16 sites
234a745 hydra spec spec: the label grammar
e06ca88 hydra spec spec: delete change:emergency
6458e1b hydra spec spec+D20: change:start is the human intent
beae33a hydra change-script labels: add change:start and berth:stale
5e3a491 hydra change-script feat(uat): CHANGE_WINDOW_ADVISORY
ca0861e hydra spec spec: the labels are print statements
49a5550 mini change-script feat(lock): deploy:staging is the lock
0a10c80 hydra gate fix(guards): check run names must be literal
f9e51b9 hydra spec spec+D19: experiment 022 defines the six protections
65a0ab8 hydra change-script fix(schedule): only one `start` per active change
c43a18f hydra spec spec: the release workflow adds two releases
6c09b23 hydra spec spec: if a hold label is present, it is NOT a scheduling issue
e64ff1c hydra spec spec: guard 5 (staging-first) and its check, deployed.py
50a3c6a hydra gate tests(e2e): the set of e2e checks is known
29f8e21 hydra change-script fix(settle): merged and productive change:release now
e5088e4 mini change-script fix(reap): the stale berth is reaped in three cases
80a5c1e hydra change-script fix(abort): change:complete was missing
c3e0be7 hydra spec spec: two non-mutually-exclusive paths through the freeze
05d2f67 hydra gate chore(gates): split the two guard tests
2adf71e mini change-script feat(watch): the change watch is built
3b8ad86 hydra change-script fix(marker): mark:prod is needed by tests
eee7d9c mini experiment experiment 024: transition matrix for the label state
8c47c8b mini change-script fix(lock): scheduler.sh needs to know when a slot is held
a0dad0d hydra spec spec: production-first and production-only get guards
57a0a5f mini gate fix(gates): take the literal check name from the log
4b5f0d4 hydra spec spec: the frozen edge is not stable; no unilateral advance
ae4c3a0 hydra change-script feat(release): transition from scheduled to released
9514db7 hydra spec spec: a change carries three properties with distinct lifetimes
8ab2e5e mini change-script fix(driver): the human intent read back correctly
8dc8221 hydra change-script feat(abort): add this one-by-one flow
78b7f22 hydra gate fix(guards): the production-only guard
db17269 hydra spec spec+D21: release:completed is the exit criterion from deploy:production
d7a4ab2 hydra gate fix(e2e): the two e2e phases and the slot rollout
3f1c149 mini spec spec: mini is a FreeBSD cell, hydra is Linux; the estate is drawn
f1a7b1a hydra change-script fix(release): emit the release:completed label
7f738de hydra change-script fix(watch): log the watch start and the timeout
b0c8e17 mini change-script feat(activate): gates/e2e.sh --pr is now available
#+end_example

*** Spot re-review sample (20 rows)

Sampled shas (for manual re-verification):

#+begin_example
0cac8af
126c03a
1dc0407
503bbe9
72592ad
77a8a92
81af5ce
944c541
9514db7
a5a2d64
ae88308
b2ea4be
b96cb12
beae33a
c43bf36
d727900
d89e7cc
e62d4fd
f3fe16d
f7ade0f
#+end_example

Spot re-review: Manual inspection of these 20 rows against the ledger should be
done to verify the verdicts are correct. (This section will be completed after
manual re-review.)

*** What a reader of the mainline would most want fixed

Based on the counts and protocol defects:

1. **The 50 missing-note gap (protocol defects).** The protocol requires notes on
   commits touching spec, gates, and change-scripts. 50 commits required notes
   and carry none. These are not bugs in the commits themselves, but gaps in the
   discipline. The author expects these to be reviewed and the decision made:
   either enforce the protocol (flag in CI), or relax the protocol (amend
   `.meta/methodology.org`). The finding belongs in that methodology document.

2. **Baseline state drifts.** Two gates run today and report failures that are not
   errors: =gates/label-audit.py= (13 findings) and =gates/label-model-coverage.py=
   (17 missing labels). The baseline was recorded at the start of the run. If these
   counts change in the next window, the maintenance debt is growing. Monitor for
   regression; if they grow, a bug-fixing sprint is needed.

3. **The label model is incomplete.** The sim runs with violation: =NoUnbookedDeploy=.
   This is a known defect documented elsewhere, not a regression. It stands as a
   debt. Resolution requires either closing the loophole in the model (change
   semantics) or aligning the pipeline to fit the model (engineer the estate).

** Verification checklist

- [x] ledger.tsv has 185 data rows (one per commit in <base>..main)
- [x] No cell holds =?=
- [x] All 185 verdict cells hold one of: confirmed, confirmed-by-note-only, unverifiable, contradicted, no-claim
- [x] All 185 evidence cells are non-empty and name observed facts
- [x] git status --short shows changes only under experiments/026-mainline-review/
- [x] Spot re-review ready (20 sampled shas listed above; manual re-verification to be performed)

** Notes on the execution

- extract.py found 30 commits with git notes; 10 have both Timeline and Reproduction sections.
- No contradicted verdicts found (none of the gate reproductions were re-run, as
  all were marked no-slow, no-host, or no-writes).
- The review followed the procedure as written; all mechanical steps completed;
  judgment application used only the decision table in step 12.
- All ledger rows have exactly 24 tab-separated fields; all verdict values are
  in the closed set.

* 2026-09-16, the mechanical pass (extract.py --mechanical)

After 027 F1 found the cheap driver had not run the per-row checks, the
survival check (Step 7) and the rules of the decision table that need no
run (1, 4, 5, 7, 8, 9, 10, 11) moved into the extractor. One command, 7.5
seconds, no model, over the same 185 commits from =d4776a6=. The driver's
ledger is kept as =ledger-driver.tsv=; =ledger.tsv= is now the mechanical
one, and the rows an agent still owes are the ten with a reproduction
command (rule 6) and the rows whose notes say they opened an issue (rule 3).

| pass       | confirmed | confirmed-by-note-only | no-claim | unverifiable | tool uses | cost   |
|------------+-----------+------------------------+----------+--------------+-----------+--------|
| driver     |         0 |                      7 |        7 |          171 |        35 | USD 0.41 |
| mechanical |        85 |                     10 |        7 |           84 |         0 | 7.5 s  |

By kind:

| kind | confirmed | confirmed-by-note-only | no-claim | unverifiable | total |
|---+---+---+---+---+---|
| apps | 31 | 0 | 0 | 8 | 39 |
| change-script | 0 | 4 | 0 | 23 | 27 |
| chore | 3 | 1 | 7 | 2 | 13 |
| docs | 13 | 0 | 0 | 19 | 32 |
| experiment | 20 | 0 | 0 | 1 | 21 |
| gate | 0 | 1 | 0 | 14 | 15 |
| model | 8 | 4 | 0 | 1 | 13 |
| research | 10 | 0 | 0 | 3 | 13 |
| spec | 0 | 0 | 0 | 13 | 13 |

By cell:

| cell | confirmed | confirmed-by-note-only | no-claim | unverifiable | total |
|---+---+---+---+---+---|
| hydra | 50 | 1 | 2 | 50 | 103 |
| mini | 35 | 9 | 5 | 34 | 83 |

The 84 unverifiable rows: 50 are the protocol defect (a note
required, none present), 29 have their added lines gone from HEAD
(superseded; the evidence names the sha), 5 added no line long enough
to grep. The protocol defects by cell: hydra 36, mini 14.

What the mechanical pass cannot say is whether a change that survives is
correct; survival is the weakest evidence the specification allowed and it
is stated as such in every evidence cell. The notes that exist are the only
stronger evidence, and fifty commits that should have them do not.
