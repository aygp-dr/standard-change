#+TITLE: Choosing a deployment target
#+SUBTITLE: What can actually attest, on this estate
#+DATE: 2026-09-13
#+OPTIONS: toc:2 num:t

* The test

From =spec.org= §C6:

#+begin_quote
A platform can attest to what is running only if it runs it.
#+end_quote

That single question sorts the candidates faster than any feature comparison,
because C6 -- a platform-attested deployment record -- is the capability the
whole model is short of, and the one that would let guard 5 stop guessing.

* The estate, surveyed 2026-09-13

| capability          | hydra                                              |
|---------------------+----------------------------------------------------|
| bastille jails      | *11 running*, thin, 14.3-RELEASE, on =10.0.0.0/24= |
| bastille releases   | 14.3-RELEASE, 14.4-RELEASE staged                  |
| bhyve               | =vm-bhyve= present; =wal-spec-vm= running (uefi, 2 cpu, 4G) |
| podman              | 5.8.1 installed -- *root only*, and runs FreeBSD jails via =ocijail=, not Linux OCI images |
| Linux containers    | *none* -- no Linux runtime on this host              |
| nginx               | 1.30.4                                             |

So "dockerized" is not available on hydra in the usual sense. What /is/
available is the thing Docker is an imitation of.

* The candidates

| target             | runs the code? | C6 | promote by identity | multi-replica | cost here |
|--------------------+----------------+----+---------------------+---------------+-----------|
| *bastille jails*   | *yes*          | *yes* | install + start    | *yes, real*   | none -- already here |
| Cloudflare Workers | *yes*          | *yes* | version ID         | gradual split | free tier, npm wrangler |
| Linux VM + Docker  | yes            | yes | image digest        | yes           | build and maintain a guest |
| GitHub Pages       | yes            | *no* -- no record, no headers | rebuild | no | already wired, weakest |

** Bastille is the strongest local answer

Not a compromise. Three properties the current setup lacks:

1. *It runs the code, so it can attest.* =jls= and =bastille list= are the
   host's own statement about what is running -- the same property that makes
   Cloudflare's =deployments list= attestation and GitHub's Deployments API
   bookkeeping. C6 is *met* here, locally, with no account.
2. *Real replicas with real identities.* Each jail has its own IP. Guard 5's
   convergence check would run against an actual fleet rather than the nginx
   round-robin stand-in of =experiments/004=, which faked a mixed fleet in one
   process space.
3. *A rollback target that survives.* A previous jail can be kept stopped
   rather than destroyed, which is a reachable rollback artifact -- the thing
   =spec.org= says =non_fast_forward= is protecting and that GitHub Pages
   cannot offer at all.

What it does not give: cross-host scheduling, image digests, or anything
resembling a registry. Promotion would be "install this commit into that jail",
which is weaker than promoting a digest and must be recorded as such.

** Cloudflare Workers is the strongest remote answer

Complementary rather than competing -- it exercises what jails cannot: a real
CDN, a gradual two-version split, and promotion by immutable version ID
(§C6). Worth having *both*, because they fail differently and the model claims
to be platform-neutral.

** Two Cloudflare shapes, priced 2026-09-14 [E from docs, [H] until run]

Asked as: could Cloudflare host =apps/= plus the front, with GitHub Actions
hitting the endpoint as staging and production? Yes, two ways, and they
differ in which primitive the model gets.

| | *Workers* -- one Worker per app plus a router Worker | *Containers* -- one image: nginx + the four node servers |
|--+--+--|
| runs the apps as-is? | nearly. =node:http= servers are supported: =nodejs_compat= + =enable_nodejs_http_server_modules= (automatic from compatibility date 2025-09-01), =httpServerHandler({port})= from =cloudflare:node= wraps =createServer=. The port is a routing key, not a socket | yes, byte for byte: the image is what =targets/bastille= installs, with nginx in front. =sockstat=/=daemon= go away with the Dockerfile |
| what needs changing | =node:fs= reads: =routes.json= (import it), =shared/oneui.js='s =readFileSync= (import), core's =/statics/= directory (a Static Assets binding). Router: =router/server.js='s =http.request= to upstreams becomes a service binding per app -- the shape =spec.org= §Cloud mirror already specifies | a Dockerfile and =targets/cloudflare/deploy.sh=; nothing in =apps/= |
| build identity | Worker *version id*, immutable; =wrangler versions list= is the platform's record (§C6) | image *digest*; the container is bound to a Worker, so the same version record fronts it |
| staging → production | =wrangler versions upload= then =wrangler versions deploy= with a percentage: gradual deployment between two versions is native (the two-colour split with a dial instead of a switch) | the same, at the Worker in front; the image behind it promotes by digest |
| =x-build-sha= | the router Worker sets it from its own version metadata; =gates/health.sh= in =header= mode works unchanged | nginx adds it as today |
| plan | Free tier for both environments at this traffic; Paid ($5/mo) for larger limits | *Workers Paid required*, $5/mo, with 375 vCPU-min, 25 GiB-h and 200 GB-h included per month; =lite= (1/16 vCPU, 256 MiB, 2 GB) is enough for a fixture estate |
| what GitHub does | Actions runs =gates/e2e.sh=, =smoke.sh= and =uat.sh= with =ROUTER_URL= at the public hostname, and =report.sh= posts the statuses guard 2 reads. The runner can see the estate, which is the one thing it cannot on hydra -- =merge-on-healthy.yml='s "settle from a host that can see the estate" stops being true | same |

The Containers row is the bastille estate on someone else's host with the
digest primitive the jails lack; the Workers row is the shape the spec wrote
for this target and costs a small port of the apps. Either one closes the
gap the README's "What exists" table names: an environment the forge can
reach. Neither is built; =wrangler= is still not installed on either host.

* Proposed shape [P -- not built]

Two jails, clearly named and destroyable, on addresses outside the existing
=10.0.0.{10,11,20,21,30,31,32,41,42,50}= allocation:

#+begin_example
sc-staging-1   the singleton berth's estate
sc-prod-1      production
sc-prod-2      a second replica, so convergence means something
#+end_example

=targets/bastille/deploy.sh <env> <groups...>= would install the tangled apps
into the jail and restart them; =rollback.sh= re-points at the previously
installed commit. The deployment record is =jls= plus a file the *jail* writes
at start, not one the deploying job writes.

* Open question for the owner

The eleven existing jails are production. This note proposes creating three
more and does not do it -- IP allocation and naming on =10.0.0.0/24= are the
owner's call, and a jail is not as free to create and destroy on a host that
is already doing real work.
